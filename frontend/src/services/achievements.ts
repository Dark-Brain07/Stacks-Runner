interface Achievement { id: string; title: string; description: string; unlocked: boolean; unlockedAt?: number; } const ACHIEVEMENTS: Achievement[] = [ { id: "first_game", title: "First Steps", description: "Play your first game", unlocked: false }, { id: "score_100", title: "Getting Started", description: "Score 100 points", unlocked: false }, { id: "score_500", title: "Rising Star", description: "Score 500 points", unlocked: false }, { id: "score_1000", title: "Champion", description: "Score 1000 points", unlocked: false }, { id: "score_5000", title: "Legend", description: "Score 5000 points", unlocked: false }, { id: "wallet_connect", title: "Web3 Ready", description: "Connect your wallet", unlocked: false }, { id: "submit_score", title: "On-Chain", description: "Submit a score on-chain", unlocked: false }, { id: "games_10", title: "Dedicated", description: "Play 10 games", unlocked: false }, { id: "games_50", title: "Veteran", description: "Play 50 games", unlocked: false }, { id: "games_100", title: "Master", description: "Play 100 games", unlocked: false } ]; const KEY = "runner-achievements"; export function getAchievements(): Achievement[] { try { return JSON.parse(localStorage.getItem(KEY) || JSON.stringify(ACHIEVEMENTS)); } catch { return ACHIEVEMENTS; } } export function unlockAchievement(id: string): Achievement | null { const list = getAchievements(); const a = list.find(x => x.id === id); if (!a || a.unlocked) return null; a.unlocked = true; a.unlockedAt = Date.now(); localStorage.setItem(KEY, JSON.stringify(list)); return a; } export function getUnlockedCount(): number { return getAchievements().filter(a => a.unlocked).length; }