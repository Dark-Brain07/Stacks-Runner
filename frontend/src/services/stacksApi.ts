const BASE_URL = "https://api.hiro.so"; export async function getAccountBalance(address: string): Promise<{ stx: number; nfts: number }> { const res = await fetch(BASE_URL + "/extended/v1/address/" + address + "/balances"); if (!res.ok) throw new Error("Failed to fetch balance"); const data = await res.json(); return { stx: parseInt(data.stx?.balance || "0", 10), nfts: Object.keys(data.non_fungible_tokens || {}).length }; } export async function getTransactionStatus(txId: string): Promise<string> { const res = await fetch(BASE_URL + "/extended/v1/tx/" + txId); if (!res.ok) throw new Error("Transaction not found"); const data = await res.json(); return data.tx_status; } export async function getContractInfo(contractId: string): Promise<{ source: string }> { const res = await fetch(BASE_URL + "/v2/contracts/source/" + contractId.replace(".", "/")); if (!res.ok) throw new Error("Contract not found"); return res.json(); } export async function getRecentBlocks(limit = 5): Promise<any[]> { const res = await fetch(BASE_URL + "/extended/v2/blocks?limit=" + limit); if (!res.ok) throw new Error("Failed to fetch blocks"); const data = await res.json(); return data.results; }