import React from "react"; export const ComboIndicator: React.FC<{ combo: number }> = ({ combo }) => { if (combo < 2) return null; const color = combo >= 10 ? "text-red-400" : combo >= 5 ? "text-yellow-400" : "text-cyan-400"; const scale = Math.min(1.5, 1 + combo * 0.05); return React.createElement("div", { className: "absolute top-20 right-4 text-center transform transition-all", style: { transform: "scale(" + scale + ")" } }, React.createElement("div", { className: "text-xs text-gray-400 uppercase" }, "Combo"), React.createElement("div", { className: "text-3xl font-black " + color }, "x" + combo)); };