import React from "react"; import { useTheme } from "../contexts/ThemeContext"; import { useAudio } from "../contexts/AudioContext"; import { t } from "../i18n"; export const SettingsPanel: React.FC<{ onClose: () => void }> = ({ onClose }) => { const { theme, toggleTheme } = useTheme(); const { volume, muted, setVolume, toggleMute } = useAudio(); return React.createElement("div", { className: "bg-gray-800 rounded-xl p-6 w-80" }, React.createElement("h2", { className: "text-xl font-bold text-white mb-4" }, t("menu.settings")), React.createElement("div", { className: "space-y-4" }, React.createElement("label", { className: "flex items-center justify-between text-gray-300" }, React.createElement("span", null, t("settings.theme")), React.createElement("button", { onClick: toggleTheme, className: "px-3 py-1 bg-purple-600 rounded text-sm" }, theme)), React.createElement("label", { className: "flex items-center justify-between text-gray-300" }, React.createElement("span", null, t("settings.sound")), React.createElement("button", { onClick: toggleMute, className: "px-3 py-1 rounded text-sm " + (muted ? "bg-red-600" : "bg-green-600") }, muted ? "Off" : "On")), React.createElement("div", { className: "text-gray-300" }, React.createElement("span", null, "Volume: " + Math.round(volume * 100) + "%"), React.createElement("input", { type: "range", min: 0, max: 100, value: volume * 100, onChange: (e: any) => setVolume(parseInt(e.target.value) / 100), className: "w-full mt-1" }))), React.createElement("button", { onClick: onClose, className: "mt-4 w-full py-2 bg-gray-700 hover:bg-gray-600 rounded text-white" }, "Close")); };