import React from "react"; interface ModalProps { isOpen: boolean; onClose: () => void; title?: string; children: React.ReactNode; } export const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children }) => { if (!isOpen) return null; return React.createElement("div", { className: "fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm", onClick: onClose }, React.createElement("div", { className: "bg-gray-800 border border-gray-700 rounded-2xl p-8 max-w-md w-full mx-4 shadow-2xl", onClick: (e: React.MouseEvent) => e.stopPropagation() }, title && React.createElement("h2", { className: "text-xl font-bold text-white mb-4" }, title), children)); };
