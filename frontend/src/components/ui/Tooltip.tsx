import React from "react"; interface TooltipProps { text: string; children: React.ReactNode; position?: "top" | "bottom"; } export const Tooltip: React.FC<TooltipProps> = ({ text, children, position = "top" }) => { const pos = position === "top" ? "bottom-full mb-2" : "top-full mt-2"; return React.createElement("div", { className: "relative group inline-block" }, children, React.createElement("span", { className: `absolute left-1/2 -translate-x-1/2 ${pos} px-2 py-1 text-xs text-white bg-gray-900 rounded opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap pointer-events-none`, role: "tooltip" }, text)); };
