import React from "react"; interface ProgressBarProps { value: number; max: number; color?: string; label?: string; } export const ProgressBar: React.FC<ProgressBarProps> = ({ value, max, color = "bg-purple-500", label }) => { const pct = Math.min(100, (value / max) * 100); return React.createElement("div", { className: "w-full" }, label && React.createElement("div", { className: "flex justify-between text-xs text-gray-400 mb-1" }, React.createElement("span", null, label), React.createElement("span", null, Math.round(pct) + "%")), React.createElement("div", { className: "w-full h-2 bg-gray-700 rounded-full overflow-hidden" }, React.createElement("div", { className: `h-full rounded-full transition-all duration-500 ${color}`, style: { width: pct + "%" } }))); };
