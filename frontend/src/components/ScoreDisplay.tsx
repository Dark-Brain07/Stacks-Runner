import React from "react"; import { formatScore } from "../utils/format"; interface ScoreDisplayProps { score: number; highScore: number; label?: string; } export const ScoreDisplay: React.FC<ScoreDisplayProps> = ({ score, highScore, label = "Score" }) => { const isNewHigh = score > 0 && score >= highScore; return React.createElement("div", { className: "flex flex-col items-center gap-1" }, React.createElement("span", { className: "text-xs text-gray-400 uppercase tracking-wider" }, label), React.createElement("span", { className: `text-2xl font-bold ${isNewHigh ? "text-yellow-400 animate-pulse" : "text-white"}` }, formatScore(score)), highScore > 0 && React.createElement("span", { className: "text-xs text-gray-500" }, "Best: " + formatScore(highScore))); };
