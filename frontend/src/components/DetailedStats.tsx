import React from 'react'; import { getStats } from '../services/statistics';
export const DetailedStats: React.FC = () => { const s = getStats(); const stats = [{ l: 'Total Games', v: s.totalGames }, { l: 'Best Score', v: s.bestScore.toLocaleString() }, { l: 'Avg Score', v: s.avgScore.toLocaleString() }, { l: 'Total Coins', v: s.coinsCollected.toLocaleString() }, { l: 'Power-Ups', v: s.powerUpsUsed }, { l: 'Play Time', v: Math.round(s.totalTime / 60000) + 'm' }]; return React.createElement('div', { className: 'grid grid-cols-2 gap-3' }, stats.map(st => React.createElement('div', { key: st.l, className: 'bg-gray-800/50 rounded-lg p-3' }, React.createElement('div', { className: 'text-xs text-gray-400' }, st.l), React.createElement('div', { className: 'text-lg font-bold text-white' }, st.v)))); };