import React from "react"; import { Badge } from "./ui/Badge"; interface SpeedIndicatorProps { speed: number; } export const SpeedIndicator: React.FC<SpeedIndicatorProps> = ({ speed }) => { const color = speed >= 2.0 ? "red" : speed >= 1.5 ? "yellow" : "green"; return React.createElement("div", { className: "flex items-center gap-2" }, React.createElement(Badge, { color }, "x" + speed.toFixed(1)), React.createElement("span", { className: "text-xs text-gray-400" }, "Speed")); };
