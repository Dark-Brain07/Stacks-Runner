export function validateCSPHeaders(headers: Record<string, string>): boolean { const required = ["X-Content-Type-Options", "X-Frame-Options"]; return required.every(h => h in headers); } export const CSP_DIRECTIVES = { "default-src": ["self"], "script-src": ["self", "unsafe-inline"], "style-src": ["self", "unsafe-inline", "https://fonts.googleapis.com"], "img-src": ["self", "data:", "blob:"], "connect-src": ["self", "https://api.hiro.so", "https://api.testnet.hiro.so"], "font-src": ["self", "https://fonts.gstatic.com"] } as const;
