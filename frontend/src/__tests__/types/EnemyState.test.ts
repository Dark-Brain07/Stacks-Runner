import{describe,it,expect}from'vitest';import{createEnemyState,updateEnemyState}from'../../types/EnemyState';
describe('EnemyState',()=>{it('creates',()=>{const s=createEnemyState('test');expect(s.id).toBe('test');expect(s.active).toBe(true);});});