import{describe,it,expect}from'vitest';import{Timeline}from'../../game/animation/Timeline';
describe('Timeline',()=>{it('starts stopped',()=>expect(new Timeline().isPlaying()).toBe(false));it('plays',()=>{const x=new Timeline();x.play();expect(x.isPlaying()).toBe(true);});it('stops and resets',()=>{const x=new Timeline();x.play();x.update(100);x.stop();expect(x.getTime()).toBe(0);});});