import{describe,it,expect}from"vitest";import{NPCReaction}from"../../game/npc/NPCReaction";
describe("NPCReaction",()=>{it("creates",()=>expect(new NPCReaction()).toBeDefined());it("toggles",()=>{const x=new NPCReaction();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new NPCReaction();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new NPCReaction();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new NPCReaction();x.dispose();expect(x.isActive()).toBe(false);});});