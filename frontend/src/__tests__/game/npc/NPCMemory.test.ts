import{describe,it,expect}from"vitest";import{NPCMemory}from"../../game/npc/NPCMemory";
describe("NPCMemory",()=>{it("creates",()=>expect(new NPCMemory()).toBeDefined());it("toggles",()=>{const x=new NPCMemory();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new NPCMemory();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new NPCMemory();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new NPCMemory();x.dispose();expect(x.isActive()).toBe(false);});});