import{describe,it,expect}from"vitest";import{NPCMerchant}from"../../game/npc/NPCMerchant";
describe("NPCMerchant",()=>{it("creates",()=>expect(new NPCMerchant()).toBeDefined());it("toggles",()=>{const x=new NPCMerchant();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new NPCMerchant();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new NPCMerchant();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new NPCMerchant();x.dispose();expect(x.isActive()).toBe(false);});});