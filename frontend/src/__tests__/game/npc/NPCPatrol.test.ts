import{describe,it,expect}from"vitest";import{NPCPatrol}from"../../game/npc/NPCPatrol";
describe("NPCPatrol",()=>{it("creates",()=>expect(new NPCPatrol()).toBeDefined());it("toggles",()=>{const x=new NPCPatrol();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new NPCPatrol();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new NPCPatrol();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new NPCPatrol();x.dispose();expect(x.isActive()).toBe(false);});});