import{describe,it,expect}from"vitest";import{PathHL}from"../../game/map3/PathHL";
describe("PathHL",()=>{it("ok",()=>expect(new PathHL()).toBeDefined());it("toggle",()=>{const x=new PathHL();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new PathHL();x.set("a",1);expect(x.get("a")).toBe(1);});});