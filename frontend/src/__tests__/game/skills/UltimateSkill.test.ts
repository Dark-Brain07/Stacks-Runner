import{describe,it,expect}from"vitest";import{UltimateSkill}from"../../game/skills/UltimateSkill";
describe("UltimateSkill",()=>{it("creates",()=>expect(new UltimateSkill()).toBeDefined());it("toggles",()=>{const x=new UltimateSkill();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new UltimateSkill();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new UltimateSkill();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new UltimateSkill();x.dispose();expect(x.isActive()).toBe(false);});});