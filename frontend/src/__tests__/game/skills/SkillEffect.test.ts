import{describe,it,expect}from"vitest";import{SkillEffect}from"../../game/skills/SkillEffect";
describe("SkillEffect",()=>{it("creates",()=>expect(new SkillEffect()).toBeDefined());it("toggles",()=>{const x=new SkillEffect();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SkillEffect();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SkillEffect();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SkillEffect();x.dispose();expect(x.isActive()).toBe(false);});});