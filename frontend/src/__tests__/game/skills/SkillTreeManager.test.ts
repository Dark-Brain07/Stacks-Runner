import{describe,it,expect}from"vitest";import{SkillTreeManager}from"../../game/skills/SkillTreeManager";
describe("SkillTreeManager",()=>{it("creates",()=>expect(new SkillTreeManager()).toBeDefined());it("toggles",()=>{const x=new SkillTreeManager();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SkillTreeManager();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SkillTreeManager();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SkillTreeManager();x.dispose();expect(x.isActive()).toBe(false);});});