import{describe,it,expect}from"vitest";import{SkillBranch}from"../../game/skills/SkillBranch";
describe("SkillBranch",()=>{it("creates",()=>expect(new SkillBranch()).toBeDefined());it("toggles",()=>{const x=new SkillBranch();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SkillBranch();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SkillBranch();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SkillBranch();x.dispose();expect(x.isActive()).toBe(false);});});