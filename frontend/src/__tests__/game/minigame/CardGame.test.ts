import{describe,it,expect}from"vitest";import{CardGame}from"../../game/minigame/CardGame";
describe("CardGame",()=>{it("creates",()=>expect(new CardGame()).toBeDefined());it("toggles",()=>{const x=new CardGame();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CardGame();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CardGame();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CardGame();x.dispose();expect(x.isActive()).toBe(false);});});