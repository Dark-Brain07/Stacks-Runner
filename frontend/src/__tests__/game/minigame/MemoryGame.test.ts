import{describe,it,expect}from"vitest";import{MemoryGame}from"../../game/minigame/MemoryGame";
describe("MemoryGame",()=>{it("creates",()=>expect(new MemoryGame()).toBeDefined());it("toggles",()=>{const x=new MemoryGame();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new MemoryGame();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new MemoryGame();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new MemoryGame();x.dispose();expect(x.isActive()).toBe(false);});});