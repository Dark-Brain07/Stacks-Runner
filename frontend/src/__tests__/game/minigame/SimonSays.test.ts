import{describe,it,expect}from"vitest";import{SimonSays}from"../../game/minigame/SimonSays";
describe("SimonSays",()=>{it("creates",()=>expect(new SimonSays()).toBeDefined());it("toggles",()=>{const x=new SimonSays();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SimonSays();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SimonSays();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SimonSays();x.dispose();expect(x.isActive()).toBe(false);});});