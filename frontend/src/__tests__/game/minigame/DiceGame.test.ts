import{describe,it,expect}from"vitest";import{DiceGame}from"../../game/minigame/DiceGame";
describe("DiceGame",()=>{it("creates",()=>expect(new DiceGame()).toBeDefined());it("toggles",()=>{const x=new DiceGame();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DiceGame();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DiceGame();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DiceGame();x.dispose();expect(x.isActive()).toBe(false);});});