import{describe,it,expect}from"vitest";import{Sandstorm}from"../../game/weather/Sandstorm";
describe("Sandstorm",()=>{it("creates",()=>expect(new Sandstorm()).toBeDefined());it("toggles",()=>{const x=new Sandstorm();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Sandstorm();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Sandstorm();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Sandstorm();x.dispose();expect(x.isActive()).toBe(false);});});