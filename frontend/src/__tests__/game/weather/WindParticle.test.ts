import{describe,it,expect}from"vitest";import{WindParticle}from"../../game/weather/WindParticle";
describe("WindParticle",()=>{it("creates",()=>expect(new WindParticle()).toBeDefined());it("toggles",()=>{const x=new WindParticle();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new WindParticle();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new WindParticle();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new WindParticle();x.dispose();expect(x.isActive()).toBe(false);});});