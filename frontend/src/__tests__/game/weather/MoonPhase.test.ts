import{describe,it,expect}from"vitest";import{MoonPhase}from"../../game/weather/MoonPhase";
describe("MoonPhase",()=>{it("creates",()=>expect(new MoonPhase()).toBeDefined());it("toggles",()=>{const x=new MoonPhase();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new MoonPhase();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new MoonPhase();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new MoonPhase();x.dispose();expect(x.isActive()).toBe(false);});});