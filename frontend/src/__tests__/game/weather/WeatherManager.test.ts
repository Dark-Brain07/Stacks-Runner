import{describe,it,expect}from"vitest";import{WeatherManager}from"../../game/weather/WeatherManager";
describe("WeatherManager",()=>{it("creates",()=>expect(new WeatherManager()).toBeDefined());it("toggles",()=>{const x=new WeatherManager();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new WeatherManager();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new WeatherManager();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new WeatherManager();x.dispose();expect(x.isActive()).toBe(false);});});