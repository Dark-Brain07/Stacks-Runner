import{describe,it,expect}from"vitest";import{FogRenderer}from"../../game/weather/FogRenderer";
describe("FogRenderer",()=>{it("creates",()=>expect(new FogRenderer()).toBeDefined());it("toggles",()=>{const x=new FogRenderer();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new FogRenderer();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new FogRenderer();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new FogRenderer();x.dispose();expect(x.isActive()).toBe(false);});});