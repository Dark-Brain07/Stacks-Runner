import{describe,it,expect}from"vitest";import{Hailstorm}from"../../game/weather/Hailstorm";
describe("Hailstorm",()=>{it("creates",()=>expect(new Hailstorm()).toBeDefined());it("toggles",()=>{const x=new Hailstorm();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Hailstorm();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Hailstorm();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Hailstorm();x.dispose();expect(x.isActive()).toBe(false);});});