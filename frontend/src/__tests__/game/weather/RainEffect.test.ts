import{describe,it,expect}from"vitest";import{RainEffect}from"../../game/weather/RainEffect";
describe("RainEffect",()=>{it("creates",()=>expect(new RainEffect()).toBeDefined());it("toggles",()=>{const x=new RainEffect();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new RainEffect();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new RainEffect();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new RainEffect();x.dispose();expect(x.isActive()).toBe(false);});});