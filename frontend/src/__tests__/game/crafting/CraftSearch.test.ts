import{describe,it,expect}from"vitest";import{CraftSearch}from"../../game/crafting/CraftSearch";
describe("CraftSearch",()=>{it("creates",()=>expect(new CraftSearch()).toBeDefined());it("toggles",()=>{const x=new CraftSearch();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CraftSearch();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CraftSearch();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CraftSearch();x.dispose();expect(x.isActive()).toBe(false);});});