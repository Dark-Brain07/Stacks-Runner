import{describe,it,expect}from"vitest";import{CraftSound}from"../../game/crafting/CraftSound";
describe("CraftSound",()=>{it("creates",()=>expect(new CraftSound()).toBeDefined());it("toggles",()=>{const x=new CraftSound();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CraftSound();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CraftSound();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CraftSound();x.dispose();expect(x.isActive()).toBe(false);});});