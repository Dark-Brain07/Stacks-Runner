import{describe,it,expect}from"vitest";import{CraftFilter}from"../../game/crafting/CraftFilter";
describe("CraftFilter",()=>{it("creates",()=>expect(new CraftFilter()).toBeDefined());it("toggles",()=>{const x=new CraftFilter();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CraftFilter();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CraftFilter();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CraftFilter();x.dispose();expect(x.isActive()).toBe(false);});});