import{describe,it,expect}from"vitest";import{CraftQueue}from"../../game/crafting/CraftQueue";
describe("CraftQueue",()=>{it("creates",()=>expect(new CraftQueue()).toBeDefined());it("toggles",()=>{const x=new CraftQueue();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CraftQueue();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CraftQueue();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CraftQueue();x.dispose();expect(x.isActive()).toBe(false);});});