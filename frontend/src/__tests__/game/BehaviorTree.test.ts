import{describe,it,expect}from'vitest';import{BehaviorTree}from'../../game/ai/BehaviorTree';
describe('BehaviorTree',()=>{it('creates instance',()=>expect(new BehaviorTree()).toBeDefined());it('enables/disables',()=>{const x=new BehaviorTree();x.disable();expect(x.isEnabled()).toBe(false);x.enable();expect(x.isEnabled()).toBe(true);});});