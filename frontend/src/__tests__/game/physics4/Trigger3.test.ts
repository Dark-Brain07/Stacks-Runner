import{describe,it,expect}from"vitest";import{Trigger3}from"../../game/physics4/Trigger3";
describe("Trigger3",()=>{it("ok",()=>expect(new Trigger3()).toBeDefined());it("toggle",()=>{const x=new Trigger3();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new Trigger3();x.set("a",1);expect(x.get("a")).toBe(1);});});