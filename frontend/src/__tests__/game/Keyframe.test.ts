import{describe,it,expect}from'vitest';import{Keyframe}from'../../game/animation/Keyframe';
describe('Keyframe',()=>{it('starts stopped',()=>expect(new Keyframe().isPlaying()).toBe(false));it('plays',()=>{const x=new Keyframe();x.play();expect(x.isPlaying()).toBe(true);});it('stops and resets',()=>{const x=new Keyframe();x.play();x.update(100);x.stop();expect(x.getTime()).toBe(0);});});