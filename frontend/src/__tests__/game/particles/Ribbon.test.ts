import{describe,it,expect}from"vitest";import{Ribbon}from"../../game/particles/Ribbon";
describe("Ribbon",()=>{it("ok",()=>expect(new Ribbon()).toBeDefined());it("toggle",()=>{const x=new Ribbon();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new Ribbon();x.set("a",1);expect(x.get("a")).toBe(1);});});