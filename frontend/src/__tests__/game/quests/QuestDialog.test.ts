import{describe,it,expect}from"vitest";import{QuestDialog}from"../../game/quests/QuestDialog";
describe("QuestDialog",()=>{it("creates",()=>expect(new QuestDialog()).toBeDefined());it("toggles",()=>{const x=new QuestDialog();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestDialog();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestDialog();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestDialog();x.dispose();expect(x.isActive()).toBe(false);});});