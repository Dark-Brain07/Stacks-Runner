import{describe,it,expect}from"vitest";import{QuestChain}from"../../game/quests/QuestChain";
describe("QuestChain",()=>{it("creates",()=>expect(new QuestChain()).toBeDefined());it("toggles",()=>{const x=new QuestChain();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestChain();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestChain();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestChain();x.dispose();expect(x.isActive()).toBe(false);});});