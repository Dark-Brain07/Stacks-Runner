import{describe,it,expect}from"vitest";import{QuestNotifier}from"../../game/quests/QuestNotifier";
describe("QuestNotifier",()=>{it("creates",()=>expect(new QuestNotifier()).toBeDefined());it("toggles",()=>{const x=new QuestNotifier();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestNotifier();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestNotifier();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestNotifier();x.dispose();expect(x.isActive()).toBe(false);});});