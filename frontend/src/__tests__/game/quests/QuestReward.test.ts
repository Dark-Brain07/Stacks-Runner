import{describe,it,expect}from"vitest";import{QuestReward}from"../../game/quests/QuestReward";
describe("QuestReward",()=>{it("creates",()=>expect(new QuestReward()).toBeDefined());it("toggles",()=>{const x=new QuestReward();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestReward();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestReward();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestReward();x.dispose();expect(x.isActive()).toBe(false);});});