import{describe,it,expect}from"vitest";import{DungeonChest}from"../../game/dungeon/DungeonChest";
describe("DungeonChest",()=>{it("creates",()=>expect(new DungeonChest()).toBeDefined());it("toggles",()=>{const x=new DungeonChest();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonChest();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonChest();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonChest();x.dispose();expect(x.isActive()).toBe(false);});});