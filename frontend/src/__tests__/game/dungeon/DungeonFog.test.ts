import{describe,it,expect}from"vitest";import{DungeonFog}from"../../game/dungeon/DungeonFog";
describe("DungeonFog",()=>{it("creates",()=>expect(new DungeonFog()).toBeDefined());it("toggles",()=>{const x=new DungeonFog();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonFog();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonFog();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonFog();x.dispose();expect(x.isActive()).toBe(false);});});