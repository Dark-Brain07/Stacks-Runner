import{describe,it,expect}from"vitest";import{DungeonLoot}from"../../game/dungeon/DungeonLoot";
describe("DungeonLoot",()=>{it("creates",()=>expect(new DungeonLoot()).toBeDefined());it("toggles",()=>{const x=new DungeonLoot();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonLoot();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonLoot();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonLoot();x.dispose();expect(x.isActive()).toBe(false);});});