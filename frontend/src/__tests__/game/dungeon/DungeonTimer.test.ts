import{describe,it,expect}from"vitest";import{DungeonTimer}from"../../game/dungeon/DungeonTimer";
describe("DungeonTimer",()=>{it("creates",()=>expect(new DungeonTimer()).toBeDefined());it("toggles",()=>{const x=new DungeonTimer();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonTimer();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonTimer();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonTimer();x.dispose();expect(x.isActive()).toBe(false);});});