import{describe,it,expect}from"vitest";import{DungeonPuzzle}from"../../game/dungeon/DungeonPuzzle";
describe("DungeonPuzzle",()=>{it("creates",()=>expect(new DungeonPuzzle()).toBeDefined());it("toggles",()=>{const x=new DungeonPuzzle();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonPuzzle();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonPuzzle();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonPuzzle();x.dispose();expect(x.isActive()).toBe(false);});});