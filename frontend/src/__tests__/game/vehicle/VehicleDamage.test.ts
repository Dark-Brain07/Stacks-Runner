import{describe,it,expect}from"vitest";import{VehicleDamage}from"../../game/vehicle/VehicleDamage";
describe("VehicleDamage",()=>{it("creates",()=>expect(new VehicleDamage()).toBeDefined());it("toggles",()=>{const x=new VehicleDamage();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleDamage();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleDamage();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleDamage();x.dispose();expect(x.isActive()).toBe(false);});});