import{describe,it,expect}from"vitest";import{VehicleAI}from"../../game/vehicle/VehicleAI";
describe("VehicleAI",()=>{it("creates",()=>expect(new VehicleAI()).toBeDefined());it("toggles",()=>{const x=new VehicleAI();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleAI();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleAI();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleAI();x.dispose();expect(x.isActive()).toBe(false);});});