import{describe,it,expect}from"vitest";import{VehicleCamera}from"../../game/vehicle/VehicleCamera";
describe("VehicleCamera",()=>{it("creates",()=>expect(new VehicleCamera()).toBeDefined());it("toggles",()=>{const x=new VehicleCamera();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleCamera();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleCamera();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleCamera();x.dispose();expect(x.isActive()).toBe(false);});});