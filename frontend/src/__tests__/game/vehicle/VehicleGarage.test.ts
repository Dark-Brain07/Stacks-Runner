import{describe,it,expect}from"vitest";import{VehicleGarage}from"../../game/vehicle/VehicleGarage";
describe("VehicleGarage",()=>{it("creates",()=>expect(new VehicleGarage()).toBeDefined());it("toggles",()=>{const x=new VehicleGarage();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleGarage();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleGarage();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleGarage();x.dispose();expect(x.isActive()).toBe(false);});});