import{describe,it,expect}from"vitest";import{VehicleController}from"../../game/vehicle/VehicleController";
describe("VehicleController",()=>{it("creates",()=>expect(new VehicleController()).toBeDefined());it("toggles",()=>{const x=new VehicleController();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleController();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleController();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleController();x.dispose();expect(x.isActive()).toBe(false);});});