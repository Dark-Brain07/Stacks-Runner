import{describe,it,expect}from"vitest";import{VehicleRank}from"../../game/vehicle/VehicleRank";
describe("VehicleRank",()=>{it("creates",()=>expect(new VehicleRank()).toBeDefined());it("toggles",()=>{const x=new VehicleRank();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleRank();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleRank();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleRank();x.dispose();expect(x.isActive()).toBe(false);});});