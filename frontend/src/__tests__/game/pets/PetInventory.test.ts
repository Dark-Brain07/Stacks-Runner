import{describe,it,expect}from"vitest";import{PetInventory}from"../../game/pets/PetInventory";
describe("PetInventory",()=>{it("creates",()=>expect(new PetInventory()).toBeDefined());it("toggles",()=>{const x=new PetInventory();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PetInventory();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PetInventory();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PetInventory();x.dispose();expect(x.isActive()).toBe(false);});});