import{describe,it,expect}from"vitest";import{PetAI}from"../../game/pets/PetAI";
describe("PetAI",()=>{it("creates",()=>expect(new PetAI()).toBeDefined());it("toggles",()=>{const x=new PetAI();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PetAI();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PetAI();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PetAI();x.dispose();expect(x.isActive()).toBe(false);});});