import{describe,it,expect}from"vitest";import{PetTraining}from"../../game/pets/PetTraining";
describe("PetTraining",()=>{it("creates",()=>expect(new PetTraining()).toBeDefined());it("toggles",()=>{const x=new PetTraining();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PetTraining();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PetTraining();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PetTraining();x.dispose();expect(x.isActive()).toBe(false);});});