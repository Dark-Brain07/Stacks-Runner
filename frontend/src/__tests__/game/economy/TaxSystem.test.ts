import{describe,it,expect}from"vitest";import{TaxSystem}from"../../game/economy/TaxSystem";
describe("TaxSystem",()=>{it("creates",()=>expect(new TaxSystem()).toBeDefined());it("toggles",()=>{const x=new TaxSystem();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new TaxSystem();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new TaxSystem();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new TaxSystem();x.dispose();expect(x.isActive()).toBe(false);});});