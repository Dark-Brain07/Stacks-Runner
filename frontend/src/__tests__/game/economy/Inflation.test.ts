import{describe,it,expect}from"vitest";import{Inflation}from"../../game/economy/Inflation";
describe("Inflation",()=>{it("creates",()=>expect(new Inflation()).toBeDefined());it("toggles",()=>{const x=new Inflation();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Inflation();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Inflation();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Inflation();x.dispose();expect(x.isActive()).toBe(false);});});