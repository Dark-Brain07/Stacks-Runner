import{describe,it,expect}from"vitest";import{Insurance}from"../../game/economy/Insurance";
describe("Insurance",()=>{it("creates",()=>expect(new Insurance()).toBeDefined());it("toggles",()=>{const x=new Insurance();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Insurance();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Insurance();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Insurance();x.dispose();expect(x.isActive()).toBe(false);});});