import{describe,it,expect}from"vitest";import{Portfolio}from"../../game/economy/Portfolio";
describe("Portfolio",()=>{it("creates",()=>expect(new Portfolio()).toBeDefined());it("toggles",()=>{const x=new Portfolio();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Portfolio();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Portfolio();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Portfolio();x.dispose();expect(x.isActive()).toBe(false);});});