import{describe,it,expect}from"vitest";import{Investment}from"../../game/economy/Investment";
describe("Investment",()=>{it("creates",()=>expect(new Investment()).toBeDefined());it("toggles",()=>{const x=new Investment();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Investment();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Investment();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Investment();x.dispose();expect(x.isActive()).toBe(false);});});