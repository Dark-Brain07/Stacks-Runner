import{describe,it,expect}from'vitest';import{XSSFilter}from'../../security/XSSFilter';
describe('XSSFilter',()=>{it('validates',()=>expect(new XSSFilter().validate('test')).toBe(true));it('sanitizes',()=>expect(new XSSFilter().sanitize('<script>')).not.toContain('<'));});