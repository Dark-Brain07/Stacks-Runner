import { describe, it, expect } from "vitest"; import { getFeatureFlags, setFeatureFlag, isFeatureEnabled, resetFeatureFlags } from "../../utils/featureFlags"; describe("feature flags", () => { beforeEach(() => resetFeatureFlags()); it("returns default flags", () => { const flags = getFeatureFlags(); expect(flags.enableSoundEffects).toBe(true); expect(flags.enableLeaderboard).toBe(false); }); it("toggles individual flags", () => { setFeatureFlag("enableLeaderboard", true); expect(isFeatureEnabled("enableLeaderboard")).toBe(true); }); it("resets to defaults", () => { setFeatureFlag("showFPSCounter", true); resetFeatureFlags(); expect(isFeatureEnabled("showFPSCounter")).toBe(false); }); });
