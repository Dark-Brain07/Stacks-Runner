import { describe, it, expect } from "vitest"; import { trackEvent, trackGameStart, trackGameOver, getEventLog } from "../../utils/analytics"; describe("analytics", () => { it("tracks events", () => { trackEvent("test_event", { key: "value" }); const log = getEventLog(); expect(log.length).toBeGreaterThan(0); expect(log[log.length - 1].name).toBe("test_event"); }); it("tracks game start", () => { trackGameStart(); const log = getEventLog(); expect(log[log.length - 1].name).toBe("game_start"); }); it("tracks game over with score", () => { trackGameOver(500); const log = getEventLog(); expect(log[log.length - 1].properties?.score).toBe(500); }); });
