import { describe, it, expect } from 'vitest'; import { validateConfig, mergeConfig, DEFAULT_CONFIG } from '../../utils/configValidator';
describe('configValidator', () => { it('accepts valid', () => expect(validateConfig({ network: 'mainnet' }).valid).toBe(true)); it('rejects invalid network', () => expect(validateConfig({ network: 'x' as any }).valid).toBe(false)); it('rejects negative retries', () => expect(validateConfig({ maxRetries: -1 }).valid).toBe(false)); it('merges', () => { const c = mergeConfig({ maxRetries: 5 }); expect(c.maxRetries).toBe(5); expect(c.apiUrl).toBe(DEFAULT_CONFIG.apiUrl); }); });