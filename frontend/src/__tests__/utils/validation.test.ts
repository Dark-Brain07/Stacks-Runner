import { describe, it, expect } from "vitest"; import { isValidStacksAddress, isValidTxId, isValidScore } from "../../utils/validation"; describe("isValidStacksAddress", () => { it("accepts valid mainnet address", () => { expect(isValidStacksAddress("SP1YH5MXTJT86BZXMFA2T51JF0QVZ8XNYV33QH6MF")).toBe(true); }); it("rejects empty string", () => { expect(isValidStacksAddress("")).toBe(false); }); }); describe("isValidTxId", () => { it("accepts valid tx id", () => { expect(isValidTxId("0x" + "a".repeat(64))).toBe(true); }); }); describe("isValidScore", () => { it("accepts valid scores", () => { expect(isValidScore(500)).toBe(true); }); it("rejects negative", () => { expect(isValidScore(-1)).toBe(false); }); });
