import { describe, it, expect } from "vitest"; import { perfMonitor } from "../../utils/performance"; describe("PerformanceMonitor", () => { it("tracks timing", () => { perfMonitor.start("test"); const dur = perfMonitor.end("test"); expect(dur).toBeGreaterThanOrEqual(0); }); it("returns 0 for unknown metric", () => { expect(perfMonitor.end("unknown")).toBe(0); }); it("resets state", () => { perfMonitor.reset(); expect(perfMonitor.getAverageFPS()).toBe(0); }); });
