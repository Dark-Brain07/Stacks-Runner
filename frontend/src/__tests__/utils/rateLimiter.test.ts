import { describe, it, expect } from "vitest"; import { RateLimiter } from "../../utils/rateLimiter"; describe("RateLimiter", () => { it("allows requests within limit", () => { const rl = new RateLimiter(3); expect(rl.canProceed()).toBe(true); expect(rl.canProceed()).toBe(true); expect(rl.canProceed()).toBe(true); }); it("blocks requests over limit", () => { const rl = new RateLimiter(2); rl.canProceed(); rl.canProceed(); expect(rl.canProceed()).toBe(false); }); it("resets tokens", () => { const rl = new RateLimiter(1); rl.canProceed(); rl.reset(); expect(rl.canProceed()).toBe(true); }); });
