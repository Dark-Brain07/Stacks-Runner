import { describe, it, expect } from "vitest"; import { getStorageItem, setStorageItem, removeStorageItem } from "../../utils/storage"; describe("storage", () => { beforeEach(() => localStorage.clear()); it("returns fallback for missing key", () => { expect(getStorageItem("missing", 42)).toBe(42); }); it("stores and retrieves objects", () => { setStorageItem("user", { name: "test" }); expect(getStorageItem("user", null)).toEqual({ name: "test" }); }); it("removes items", () => { setStorageItem("key", "val"); removeStorageItem("key"); expect(getStorageItem("key", null)).toBeNull(); }); });
