import { describe, it, expect } from "vitest"; import { clamp, lerp, randomInt, distance } from "../../utils/math"; describe("clamp", () => { it("clamps below min", () => { expect(clamp(-5, 0, 10)).toBe(0); }); it("clamps above max", () => { expect(clamp(15, 0, 10)).toBe(10); }); it("passes through in range", () => { expect(clamp(5, 0, 10)).toBe(5); }); }); describe("lerp", () => { it("interpolates halfway", () => { expect(lerp(0, 10, 0.5)).toBe(5); }); }); describe("randomInt", () => { it("returns integer in range", () => { const r = randomInt(1, 10); expect(r).toBeGreaterThanOrEqual(1); expect(r).toBeLessThanOrEqual(10); }); }); describe("distance", () => { it("calculates 3-4-5 triangle", () => { expect(distance(0, 0, 3, 4)).toBe(5); }); });
