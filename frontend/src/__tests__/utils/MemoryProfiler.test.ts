import{describe,it,expect}from'vitest';import{MemoryProfiler}from'../../utils/profiling/MemoryProfiler';
describe('MemoryProfiler',()=>{it('records',()=>{const p=new MemoryProfiler();p.start();p.record(10);const r=p.stop();expect(r.count).toBe(1);});});