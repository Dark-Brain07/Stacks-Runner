import{describe,it,expect}from'vitest';import{PauseScreenScene}from'../../scenes/PauseScreenScene';
describe('PauseScreenScene',()=>{it('starts inactive',()=>expect(new PauseScreenScene().isActive()).toBe(false));it('activates on enter',()=>{const s=new PauseScreenScene();s.onEnter();expect(s.isActive()).toBe(true);});it('deactivates on exit',()=>{const s=new PauseScreenScene();s.onEnter();s.onExit();expect(s.isActive()).toBe(false);});});