import{describe,it,expect}from"vitest";import{GenderInflector}from"../../i18n/core/GenderInflector";
describe("GenderInflector",()=>{it("creates",()=>expect(new GenderInflector()).toBeDefined());it("toggles",()=>{const x=new GenderInflector();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new GenderInflector();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new GenderInflector();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new GenderInflector();x.dispose();expect(x.isActive()).toBe(false);});});