import{describe,it,expect}from"vitest";import{ContextResolver}from"../../i18n/core/ContextResolver";
describe("ContextResolver",()=>{it("creates",()=>expect(new ContextResolver()).toBeDefined());it("toggles",()=>{const x=new ContextResolver();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new ContextResolver();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new ContextResolver();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new ContextResolver();x.dispose();expect(x.isActive()).toBe(false);});});