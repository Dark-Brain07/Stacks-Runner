import{describe,it,expect}from"vitest";import{DateFormatter}from"../../i18n/core/DateFormatter";
describe("DateFormatter",()=>{it("creates",()=>expect(new DateFormatter()).toBeDefined());it("toggles",()=>{const x=new DateFormatter();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DateFormatter();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DateFormatter();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DateFormatter();x.dispose();expect(x.isActive()).toBe(false);});});