import{describe,it,expect}from"vitest";import{ListFormatter}from"../../i18n/core/ListFormatter";
describe("ListFormatter",()=>{it("creates",()=>expect(new ListFormatter()).toBeDefined());it("toggles",()=>{const x=new ListFormatter();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new ListFormatter();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new ListFormatter();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new ListFormatter();x.dispose();expect(x.isActive()).toBe(false);});});