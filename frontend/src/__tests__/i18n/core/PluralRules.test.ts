import{describe,it,expect}from"vitest";import{PluralRules}from"../../i18n/core/PluralRules";
describe("PluralRules",()=>{it("creates",()=>expect(new PluralRules()).toBeDefined());it("toggles",()=>{const x=new PluralRules();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PluralRules();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PluralRules();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PluralRules();x.dispose();expect(x.isActive()).toBe(false);});});