import{describe,it,expect}from"vitest";
describe("stress 3",()=>{it("handles load",()=>{const arr=Array.from({length:100000},(_,j)=>({id:j,val:j*3}));const filtered=arr.filter(x=>x.val%3===0);expect(filtered.length).toBeGreaterThan(0);});});