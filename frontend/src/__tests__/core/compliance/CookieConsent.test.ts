import{describe,it,expect}from"vitest";import{CookieConsent}from"../../core/compliance/CookieConsent";
describe("CookieConsent",()=>{it("creates",()=>expect(new CookieConsent()).toBeDefined());it("toggles",()=>{const x=new CookieConsent();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CookieConsent();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CookieConsent();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CookieConsent();x.dispose();expect(x.isActive()).toBe(false);});});