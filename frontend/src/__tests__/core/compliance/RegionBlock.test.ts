import{describe,it,expect}from"vitest";import{RegionBlock}from"../../core/compliance/RegionBlock";
describe("RegionBlock",()=>{it("creates",()=>expect(new RegionBlock()).toBeDefined());it("toggles",()=>{const x=new RegionBlock();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new RegionBlock();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new RegionBlock();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new RegionBlock();x.dispose();expect(x.isActive()).toBe(false);});});