import{describe,it,expect}from"vitest";import{ConsentManager}from"../../core/compliance/ConsentManager";
describe("ConsentManager",()=>{it("creates",()=>expect(new ConsentManager()).toBeDefined());it("toggles",()=>{const x=new ConsentManager();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new ConsentManager();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new ConsentManager();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new ConsentManager();x.dispose();expect(x.isActive()).toBe(false);});});