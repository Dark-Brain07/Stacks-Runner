import{describe,it,expect}from"vitest";import{Snapshot3}from"../../core/serial2/Snapshot3";
describe("Snapshot3",()=>{it("ok",()=>expect(new Snapshot3()).toBeDefined());it("toggle",()=>{const x=new Snapshot3();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new Snapshot3();x.set("a",1);expect(x.get("a")).toBe(1);});});