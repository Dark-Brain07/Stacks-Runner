import{describe,it,expect}from"vitest";import{Verification}from"../../core/migration/Verification";
describe("Verification",()=>{it("creates",()=>expect(new Verification()).toBeDefined());it("toggles",()=>{const x=new Verification();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Verification();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Verification();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Verification();x.dispose();expect(x.isActive()).toBe(false);});});