import{describe,it,expect}from"vitest";import{DataTransformer}from"../../core/migration/DataTransformer";
describe("DataTransformer",()=>{it("creates",()=>expect(new DataTransformer()).toBeDefined());it("toggles",()=>{const x=new DataTransformer();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DataTransformer();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DataTransformer();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DataTransformer();x.dispose();expect(x.isActive()).toBe(false);});});