import{describe,it,expect}from"vitest";import{SchemaDiff}from"../../core/migration/SchemaDiff";
describe("SchemaDiff",()=>{it("creates",()=>expect(new SchemaDiff()).toBeDefined());it("toggles",()=>{const x=new SchemaDiff();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SchemaDiff();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SchemaDiff();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SchemaDiff();x.dispose();expect(x.isActive()).toBe(false);});});