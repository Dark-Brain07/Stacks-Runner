import{describe,it,expect}from"vitest";import{LbSync}from"../../services/multiplayer/LbSync";
describe("LbSync",()=>{it("ok",()=>expect(new LbSync()).toBeDefined());it("toggle",()=>{const x=new LbSync();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new LbSync();x.set("a",1);expect(x.get("a")).toBe(1);});});