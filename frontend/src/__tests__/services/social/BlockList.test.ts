import{describe,it,expect}from"vitest";import{BlockList}from"../../services/social/BlockList";
describe("BlockList",()=>{it("creates",()=>expect(new BlockList()).toBeDefined());it("toggles",()=>{const x=new BlockList();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new BlockList();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new BlockList();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new BlockList();x.dispose();expect(x.isActive()).toBe(false);});});