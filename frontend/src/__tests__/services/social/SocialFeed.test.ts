import{describe,it,expect}from"vitest";import{SocialFeed}from"../../services/social/SocialFeed";
describe("SocialFeed",()=>{it("creates",()=>expect(new SocialFeed()).toBeDefined());it("toggles",()=>{const x=new SocialFeed();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SocialFeed();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SocialFeed();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SocialFeed();x.dispose();expect(x.isActive()).toBe(false);});});