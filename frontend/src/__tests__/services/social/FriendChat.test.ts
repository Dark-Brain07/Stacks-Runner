import{describe,it,expect}from"vitest";import{FriendChat}from"../../services/social/FriendChat";
describe("FriendChat",()=>{it("creates",()=>expect(new FriendChat()).toBeDefined());it("toggles",()=>{const x=new FriendChat();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new FriendChat();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new FriendChat();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new FriendChat();x.dispose();expect(x.isActive()).toBe(false);});});