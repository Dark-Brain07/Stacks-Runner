import{describe,it,expect}from"vitest";import{ProfileCard}from"../../services/social/ProfileCard";
describe("ProfileCard",()=>{it("creates",()=>expect(new ProfileCard()).toBeDefined());it("toggles",()=>{const x=new ProfileCard();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new ProfileCard();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new ProfileCard();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new ProfileCard();x.dispose();expect(x.isActive()).toBe(false);});});