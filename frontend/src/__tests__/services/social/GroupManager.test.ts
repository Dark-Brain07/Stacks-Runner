import{describe,it,expect}from"vitest";import{GroupManager}from"../../services/social/GroupManager";
describe("GroupManager",()=>{it("creates",()=>expect(new GroupManager()).toBeDefined());it("toggles",()=>{const x=new GroupManager();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new GroupManager();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new GroupManager();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new GroupManager();x.dispose();expect(x.isActive()).toBe(false);});});