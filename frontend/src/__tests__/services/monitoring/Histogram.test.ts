import{describe,it,expect}from"vitest";import{Histogram}from"../../services/monitoring/Histogram";
describe("Histogram",()=>{it("creates",()=>expect(new Histogram()).toBeDefined());it("toggles",()=>{const x=new Histogram();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Histogram();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Histogram();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Histogram();x.dispose();expect(x.isActive()).toBe(false);});});