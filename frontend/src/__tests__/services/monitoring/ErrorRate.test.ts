import{describe,it,expect}from"vitest";import{ErrorRate}from"../../services/monitoring/ErrorRate";
describe("ErrorRate",()=>{it("creates",()=>expect(new ErrorRate()).toBeDefined());it("toggles",()=>{const x=new ErrorRate();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new ErrorRate();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new ErrorRate();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new ErrorRate();x.dispose();expect(x.isActive()).toBe(false);});});