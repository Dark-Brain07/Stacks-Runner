import{describe,it,expect}from"vitest";import{RequestLog}from"../../services/monitoring/RequestLog";
describe("RequestLog",()=>{it("creates",()=>expect(new RequestLog()).toBeDefined());it("toggles",()=>{const x=new RequestLog();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new RequestLog();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new RequestLog();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new RequestLog();x.dispose();expect(x.isActive()).toBe(false);});});