import{describe,it,expect}from"vitest";import{GuildBuff}from"../../services/guild/GuildBuff";
describe("GuildBuff",()=>{it("creates",()=>expect(new GuildBuff()).toBeDefined());it("toggles",()=>{const x=new GuildBuff();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new GuildBuff();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new GuildBuff();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new GuildBuff();x.dispose();expect(x.isActive()).toBe(false);});});