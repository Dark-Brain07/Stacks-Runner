import { describe, it, expect } from "vitest"; import { getTodayChallenge } from "../../services/dailyChallenge"; describe("daily challenge", () => { beforeEach(() => localStorage.clear()); it("returns a challenge for today", () => { const c = getTodayChallenge(); expect(c.date).toBe(new Date().toISOString().split("T")[0]); expect(c.targetScore).toBeGreaterThan(0); expect(c.completed).toBe(false); }); it("returns consistent challenge for same day", () => { const c1 = getTodayChallenge(); const c2 = getTodayChallenge(); expect(c1.targetScore).toBe(c2.targetScore); }); });
