import { describe, it, expect } from "vitest"; import { t, setLocale, getLocale, getAvailableLocales } from "../../i18n"; describe("i18n", () => { it("returns English by default", () => { setLocale("en"); expect(t("menu.play")).toBe("Play Game"); }); it("switches to Spanish", () => { setLocale("es"); expect(t("menu.play")).toBe("Jugar"); setLocale("en"); }); it("falls back to key for unknown", () => { expect(t("nonexistent.key")).toBe("nonexistent.key"); }); it("lists available locales", () => { expect(getAvailableLocales()).toContain("en"); expect(getAvailableLocales()).toContain("es"); }); it("returns current locale", () => { setLocale("en"); expect(getLocale()).toBe("en"); }); });
