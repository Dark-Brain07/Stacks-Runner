import{describe,it,expect}from"vitest";import{AssetPipe2}from"../../tools2/AssetPipe2";
describe("AssetPipe2",()=>{it("ok",()=>expect(new AssetPipe2()).toBeDefined());it("toggle",()=>{const x=new AssetPipe2();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new AssetPipe2();x.set("a",1);expect(x.get("a")).toBe(1);});});