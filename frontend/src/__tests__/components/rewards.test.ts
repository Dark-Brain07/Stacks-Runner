import { describe, it, expect } from "vitest"; import { REWARD_TIERS } from "../../types"; describe("reward tiers", () => { it("has three tiers", () => { expect(REWARD_TIERS).toHaveLength(3); }); it("tiers are sorted by score", () => { for (let i = 1; i < REWARD_TIERS.length; i++) { expect(REWARD_TIERS[i].requiredScore).toBeGreaterThan(REWARD_TIERS[i-1].requiredScore); } }); it("each tier has label", () => { REWARD_TIERS.forEach(t => expect(t.label).toBeTruthy()); }); });
