import { describe, it, expect } from "vitest"; import { COLORS } from "../../game/colors"; describe("game colors", () => { it("has player colors defined", () => { expect(COLORS.bodyMain).toBeDefined(); expect(COLORS.suitGlow).toBeDefined(); }); it("has environment colors", () => { expect(COLORS.groundDirt).toBeDefined(); expect(COLORS.grassBase).toBeDefined(); }); it("has particle arrays", () => { expect(COLORS.trailColors).toHaveLength(3); expect(COLORS.coinBurst).toHaveLength(3); }); });
