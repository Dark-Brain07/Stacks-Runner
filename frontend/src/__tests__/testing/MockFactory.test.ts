import{describe,it,expect}from'vitest';import{MockFactory}from'../../testing/MockFactory';
describe('MockFactory',()=>{it('runs tests',()=>{const x=new MockFactory();const r=x.run('test1',()=>{});expect(r.pass).toBe(true);});it('tracks results',()=>{const x=new MockFactory();x.run('a',()=>{});x.run('b',()=>{throw new Error();});expect(x.getSummary().passed).toBe(1);expect(x.getSummary().failed).toBe(1);});});