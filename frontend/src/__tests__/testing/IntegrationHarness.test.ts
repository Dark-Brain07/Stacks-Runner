import{describe,it,expect}from'vitest';import{IntegrationHarness}from'../../testing/IntegrationHarness';
describe('IntegrationHarness',()=>{it('runs tests',()=>{const x=new IntegrationHarness();const r=x.run('test1',()=>{});expect(r.pass).toBe(true);});it('tracks results',()=>{const x=new IntegrationHarness();x.run('a',()=>{});x.run('b',()=>{throw new Error();});expect(x.getSummary().passed).toBe(1);expect(x.getSummary().failed).toBe(1);});});