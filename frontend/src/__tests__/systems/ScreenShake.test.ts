import { describe, it, expect } from "vitest"; import { ScreenShake } from "../../game/systems/ScreenShake"; describe("ScreenShake", () => { it("starts inactive", () => { const ss = new ScreenShake(); expect(ss.isActive()).toBe(false); expect(ss.update(16)).toEqual({ x: 0, y: 0 }); }); it("activates on trigger", () => { const ss = new ScreenShake(); ss.trigger(10, 500); expect(ss.isActive()).toBe(true); }); it("decays over duration", () => { const ss = new ScreenShake(); ss.trigger(10, 100); ss.update(100); expect(ss.isActive()).toBe(false); }); });
