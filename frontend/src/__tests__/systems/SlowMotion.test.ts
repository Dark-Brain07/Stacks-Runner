import { describe, it, expect } from "vitest"; import { SlowMotion } from "../../game/systems/SlowMotion"; describe("SlowMotion", () => { it("starts inactive", () => { const sm = new SlowMotion(); expect(sm.isActive()).toBe(false); expect(sm.update(16)).toBe(1); }); it("returns reduced time scale when active", () => { const sm = new SlowMotion(); sm.trigger(0.3, 1000); expect(sm.update(16)).toBe(0.3); }); it("deactivates after duration", () => { const sm = new SlowMotion(); sm.trigger(0.3, 100); sm.update(100); expect(sm.isActive()).toBe(false); }); });
