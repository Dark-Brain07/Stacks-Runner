import{describe,it,expect}from"vitest";
describe("e2e flow 8",()=>{it("full cycle",()=>{const state={hp:100,score:0,wave:8};state.score+=state.wave*50;state.hp-=8;expect(state.score).toBe(8*50);expect(state.hp).toBe(100-8);});});