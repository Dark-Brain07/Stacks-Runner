import{describe,it,expect}from"vitest";
describe("e2e flow 7",()=>{it("full cycle",()=>{const state={hp:100,score:0,wave:7};state.score+=state.wave*50;state.hp-=7;expect(state.score).toBe(7*50);expect(state.hp).toBe(100-7);});});