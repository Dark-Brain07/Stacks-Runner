import{describe,it,expect}from"vitest";
describe("e2e flow 20",()=>{it("full cycle",()=>{const state={hp:100,score:0,wave:20};state.score+=state.wave*50;state.hp-=20;expect(state.score).toBe(20*50);expect(state.hp).toBe(100-20);});});