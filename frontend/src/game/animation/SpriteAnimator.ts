export class SpriteAnimator{private frame=0;private elapsed=0;constructor(private frameCount:number,private frameDuration:number,private loop=true){}update(dt:number):number{this.elapsed+=dt;if(this.elapsed>=this.frameDuration){this.elapsed=0;this.frame++;if(this.frame>=this.frameCount){this.frame=this.loop?0:this.frameCount-1;}}return this.frame;}reset(){this.frame=0;this.elapsed=0;}getCurrentFrame():number{return this.frame;}}