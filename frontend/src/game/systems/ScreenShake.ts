export class ScreenShake { private intensity = 0; private duration = 0; private elapsed = 0; trigger(intensity = 5, duration = 200): void { this.intensity = intensity; this.duration = duration; this.elapsed = 0; } update(dt: number): { x: number; y: number } { if (this.elapsed >= this.duration) return { x: 0, y: 0 }; this.elapsed += dt; const remaining = 1 - this.elapsed / this.duration; return { x: (Math.random() - 0.5) * 2 * this.intensity * remaining, y: (Math.random() - 0.5) * 2 * this.intensity * remaining }; } isActive(): boolean { return this.elapsed < this.duration; } reset(): void { this.intensity = 0; this.duration = 0; this.elapsed = 0; } }