export class ComboSystem { private combo = 0; private lastHitTime = 0; private comboWindow = 2000; hit(): number { const now = Date.now(); if (now - this.lastHitTime > this.comboWindow) this.combo = 0; this.combo++; this.lastHitTime = now; return this.combo; } getMultiplier(): number { if (this.combo >= 10) return 3; if (this.combo >= 5) return 2; if (this.combo >= 3) return 1.5; return 1; } getCombo(): number { return this.combo; } reset(): void { this.combo = 0; this.lastHitTime = 0; } }