export class DifficultyScaler { private baseSpeed: number; private elapsed = 0; constructor(baseSpeed = 4) { this.baseSpeed = baseSpeed; } update(dt: number): void { this.elapsed += dt; } getSpeed(): number { return this.baseSpeed + Math.floor(this.elapsed / 30000) * 0.5; } getSpawnRate(): number { return Math.max(500, 1500 - Math.floor(this.elapsed / 20000) * 100); } getCoinChance(): number { return Math.min(0.4, 0.15 + this.elapsed / 300000); } getLevel(): number { return Math.floor(this.elapsed / 60000) + 1; } reset(): void { this.elapsed = 0; } }