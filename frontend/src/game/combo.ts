export class ComboTracker{private count=0;private lastTime=0;private window:number;constructor(windowMs=2000){this.window=windowMs;}hit(){const now=Date.now();if(now-this.lastTime>this.window)this.count=0;this.count++;this.lastTime=now;}getCount():number{const now=Date.now();if(now-this.lastTime>this.window){this.count=0;}return this.count;}getMultiplier():number{const c=this.getCount();if(c<3)return 1;if(c<5)return 1.5;if(c<10)return 2;return 3;}reset(){this.count=0;}}