export interface GameConfig { difficulty: "easy" | "normal" | "hard"; soundEnabled: boolean; showFPS: boolean; touchControls: boolean; } export interface DifficultyParams { speed: number; spawnRate: number; coinFrequency: number; obstacleGap: number; } export const DIFFICULTY_PRESETS: Record<string, DifficultyParams> = { easy: { speed: 3, spawnRate: 2000, coinFrequency: 1000, obstacleGap: 250 }, normal: { speed: 4, spawnRate: 1500, coinFrequency: 1200, obstacleGap: 200 }, hard: { speed: 6, spawnRate: 800, coinFrequency: 1500, obstacleGap: 150 } };
