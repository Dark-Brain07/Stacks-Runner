import { useState, useEffect, useCallback } from "react"; export function useCountdown(initial: number) { const [count, setCount] = useState(initial); const [running, setRunning] = useState(false); useEffect(() => { if (!running || count <= 0) return; const timer = setTimeout(() => setCount(c => c - 1), 1000); return () => clearTimeout(timer); }, [count, running]); const start = useCallback(() => { setCount(initial); setRunning(true); }, [initial]); const stop = useCallback(() => setRunning(false), []); const reset = useCallback(() => { setCount(initial); setRunning(false); }, [initial]); return { count, running, start, stop, reset }; }