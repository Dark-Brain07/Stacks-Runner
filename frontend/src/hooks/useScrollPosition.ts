import { useState, useEffect } from "react"; export function useScrollPosition() { const [pos, setPos] = useState({ x: window.scrollX, y: window.scrollY }); useEffect(() => { const handler = () => setPos({ x: window.scrollX, y: window.scrollY }); window.addEventListener("scroll", handler, { passive: true }); return () => window.removeEventListener("scroll", handler); }, []); return pos; }