export class SlowMotion { private active = false; private timeScale = 1; private duration = 0; private elapsed = 0; trigger(scale = 0.3, duration = 1000): void { this.active = true; this.timeScale = scale; this.duration = duration; this.elapsed = 0; } update(dt: number): number { if (!this.active) return 1; this.elapsed += dt; if (this.elapsed >= this.duration) { this.active = false; this.timeScale = 1; return 1; } return this.timeScale; } isActive(): boolean { return this.active; } reset(): void { this.active = false; this.timeScale = 1; } }