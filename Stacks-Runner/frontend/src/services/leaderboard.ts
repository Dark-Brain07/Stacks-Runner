interface ScoreEntry { address: string; score: number; timestamp: number; } const STORAGE_KEY = "runner-leaderboard"; export function getLeaderboard(): ScoreEntry[] { try { return JSON.parse(localStorage.getItem(STORAGE_KEY) || "[]"); } catch { return []; } } export function addToLeaderboard(address: string, score: number): void { const board = getLeaderboard(); board.push({ address, score, timestamp: Date.now() }); board.sort((a, b) => b.score - a.score); const top50 = board.slice(0, 50); localStorage.setItem(STORAGE_KEY, JSON.stringify(top50)); } export function getPlayerRank(address: string): number { const board = getLeaderboard(); const idx = board.findIndex(e => e.address === address); return idx === -1 ? -1 : idx + 1; } export function clearLeaderboard(): void { localStorage.removeItem(STORAGE_KEY); }