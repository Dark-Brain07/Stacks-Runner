import{describe,it,expect}from"vitest";
describe("stress 20",()=>{it("handles load",()=>{const arr=Array.from({length:100000},(_,j)=>({id:j,val:j*20}));const filtered=arr.filter(x=>x.val%20===0);expect(filtered.length).toBeGreaterThan(0);});});