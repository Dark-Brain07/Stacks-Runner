import{describe,it,expect}from"vitest";
describe("stress 1",()=>{it("handles load",()=>{const arr=Array.from({length:100000},(_,j)=>({id:j,val:j*1}));const filtered=arr.filter(x=>x.val%1===0);expect(filtered.length).toBeGreaterThan(0);});});