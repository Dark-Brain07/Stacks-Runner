import { describe, it, expect } from "vitest"; import { getHighScore, setHighScore } from "../../utils/scores"; describe("score manager", () => { beforeEach(() => localStorage.clear()); it("returns 0 when no score saved", () => { expect(getHighScore()).toBe(0); }); it("updates high score when higher", () => { expect(setHighScore(100)).toBe(true); expect(getHighScore()).toBe(100); }); it("does not update when lower", () => { setHighScore(100); expect(setHighScore(50)).toBe(false); expect(getHighScore()).toBe(100); }); });
