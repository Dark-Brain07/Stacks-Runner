import { describe, it, expect } from "vitest"; import { GameEventEmitter } from "../../utils/events"; describe("GameEventEmitter", () => { it("emits events to listeners", () => { const emitter = new GameEventEmitter(); let received = false; emitter.on("test", () => { received = true; }); emitter.emit("test"); expect(received).toBe(true); }); it("removes listeners", () => { const emitter = new GameEventEmitter(); const fn = () => {}; emitter.on("test", fn); emitter.off("test", fn); }); it("clears all listeners", () => { const emitter = new GameEventEmitter(); emitter.on("a", () => {}); emitter.on("b", () => {}); emitter.removeAll(); }); });
