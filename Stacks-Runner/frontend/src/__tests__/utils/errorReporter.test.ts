import { describe, it, expect } from "vitest"; import { reportError, getErrorReports, clearErrorReports } from "../../utils/errorReporter"; describe("errorReporter", () => { it("captures error reports", () => { clearErrorReports(); reportError(new Error("test error"), "TestComponent"); const reports = getErrorReports(); expect(reports.length).toBe(1); expect(reports[0].message).toBe("test error"); expect(reports[0].component).toBe("TestComponent"); }); it("clears reports", () => { reportError(new Error("x")); clearErrorReports(); expect(getErrorReports().length).toBe(0); }); });
