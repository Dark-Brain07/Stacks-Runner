import { describe, it, expect, vi } from 'vitest'; import { ShortcutManager } from '../../utils/shortcuts';
describe('ShortcutManager', () => { it('fires handlers', () => { const sm = new ShortcutManager(); const fn = vi.fn(); sm.register({ key: 'p', description: 'Pause', handler: fn }); sm.handleKeyDown(new KeyboardEvent('keydown', { key: 'p' })); expect(fn).toHaveBeenCalled(); }); it('ignores when disabled', () => { const sm = new ShortcutManager(); const fn = vi.fn(); sm.register({ key: 'p', description: 'Pause', handler: fn }); sm.disable(); sm.handleKeyDown(new KeyboardEvent('keydown', { key: 'p' })); expect(fn).not.toHaveBeenCalled(); }); });