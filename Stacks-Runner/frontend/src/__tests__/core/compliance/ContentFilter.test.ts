import{describe,it,expect}from"vitest";import{ContentFilter}from"../../core/compliance/ContentFilter";
describe("ContentFilter",()=>{it("creates",()=>expect(new ContentFilter()).toBeDefined());it("toggles",()=>{const x=new ContentFilter();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new ContentFilter();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new ContentFilter();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new ContentFilter();x.dispose();expect(x.isActive()).toBe(false);});});