import{describe,it,expect}from"vitest";import{DataExport}from"../../core/compliance/DataExport";
describe("DataExport",()=>{it("creates",()=>expect(new DataExport()).toBeDefined());it("toggles",()=>{const x=new DataExport();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DataExport();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DataExport();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DataExport();x.dispose();expect(x.isActive()).toBe(false);});});