import{describe,it,expect}from"vitest";import{DataDeletion}from"../../core/compliance/DataDeletion";
describe("DataDeletion",()=>{it("creates",()=>expect(new DataDeletion()).toBeDefined());it("toggles",()=>{const x=new DataDeletion();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DataDeletion();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DataDeletion();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DataDeletion();x.dispose();expect(x.isActive()).toBe(false);});});