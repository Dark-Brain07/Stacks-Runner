import{describe,it,expect}from"vitest";import{PrivacyPolicy}from"../../core/compliance/PrivacyPolicy";
describe("PrivacyPolicy",()=>{it("creates",()=>expect(new PrivacyPolicy()).toBeDefined());it("toggles",()=>{const x=new PrivacyPolicy();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PrivacyPolicy();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PrivacyPolicy();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PrivacyPolicy();x.dispose();expect(x.isActive()).toBe(false);});});