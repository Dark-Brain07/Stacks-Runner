import{describe,it,expect}from"vitest";import{Rollback4}from"../../core/migration/Rollback4";
describe("Rollback4",()=>{it("creates",()=>expect(new Rollback4()).toBeDefined());it("toggles",()=>{const x=new Rollback4();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Rollback4();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Rollback4();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Rollback4();x.dispose();expect(x.isActive()).toBe(false);});});