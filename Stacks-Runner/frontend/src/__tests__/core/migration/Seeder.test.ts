import{describe,it,expect}from"vitest";import{Seeder}from"../../core/migration/Seeder";
describe("Seeder",()=>{it("creates",()=>expect(new Seeder()).toBeDefined());it("toggles",()=>{const x=new Seeder();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Seeder();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Seeder();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Seeder();x.dispose();expect(x.isActive()).toBe(false);});});