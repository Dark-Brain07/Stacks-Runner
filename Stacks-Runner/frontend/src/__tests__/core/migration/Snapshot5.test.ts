import{describe,it,expect}from"vitest";import{Snapshot5}from"../../core/migration/Snapshot5";
describe("Snapshot5",()=>{it("creates",()=>expect(new Snapshot5()).toBeDefined());it("toggles",()=>{const x=new Snapshot5();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Snapshot5();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Snapshot5();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Snapshot5();x.dispose();expect(x.isActive()).toBe(false);});});