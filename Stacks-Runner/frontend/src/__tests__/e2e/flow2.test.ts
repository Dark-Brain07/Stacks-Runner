import{describe,it,expect}from"vitest";
describe("e2e flow 2",()=>{it("full cycle",()=>{const state={hp:100,score:0,wave:2};state.score+=state.wave*50;state.hp-=2;expect(state.score).toBe(2*50);expect(state.hp).toBe(100-2);});});