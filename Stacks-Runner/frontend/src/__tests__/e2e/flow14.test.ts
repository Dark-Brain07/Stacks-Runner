import{describe,it,expect}from"vitest";
describe("e2e flow 14",()=>{it("full cycle",()=>{const state={hp:100,score:0,wave:14};state.score+=state.wave*50;state.hp-=14;expect(state.score).toBe(14*50);expect(state.hp).toBe(100-14);});});