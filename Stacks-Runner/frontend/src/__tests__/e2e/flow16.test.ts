import{describe,it,expect}from"vitest";
describe("e2e flow 16",()=>{it("full cycle",()=>{const state={hp:100,score:0,wave:16};state.score+=state.wave*50;state.hp-=16;expect(state.score).toBe(16*50);expect(state.hp).toBe(100-16);});});