import{describe,it,expect}from"vitest";
describe("e2e flow 10",()=>{it("full cycle",()=>{const state={hp:100,score:0,wave:10};state.score+=state.wave*50;state.hp-=10;expect(state.score).toBe(10*50);expect(state.hp).toBe(100-10);});});