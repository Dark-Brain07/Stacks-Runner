import{describe,it,expect}from'vitest';import{PowerUpStore}from'../../stores/PowerUpStore';
describe('PowerUpStore',()=>{it('gets and sets',()=>{const x=new PowerUpStore();x.set('a',1);expect(x.get('a')).toBe(1);});it('clears',()=>{const x=new PowerUpStore();x.set('b',2);x.clear();expect(x.has('b')).toBe(false);});});