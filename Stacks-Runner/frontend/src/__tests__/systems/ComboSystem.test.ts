import { describe, it, expect } from "vitest"; import { ComboSystem } from "../../game/systems/ComboSystem"; describe("ComboSystem", () => { it("increments combo on hit", () => { const cs = new ComboSystem(); cs.hit(); cs.hit(); expect(cs.getCombo()).toBe(2); }); it("returns multiplier based on combo", () => { const cs = new ComboSystem(); expect(cs.getMultiplier()).toBe(1); for (let i = 0; i < 5; i++) cs.hit(); expect(cs.getMultiplier()).toBe(2); }); it("resets combo", () => { const cs = new ComboSystem(); cs.hit(); cs.hit(); cs.reset(); expect(cs.getCombo()).toBe(0); }); });
