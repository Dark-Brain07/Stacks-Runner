import{describe,it,expect}from'vitest';import{ErrorReporter}from'../../analytics/ErrorReporter';
describe('ErrorReporter',()=>{it('tracks events',()=>{const x=new ErrorReporter();x.track('click');expect(x.getMetrics().total).toBe(1);});it('flushes',()=>{const x=new ErrorReporter();x.track('a');const d=x.flush();expect(d).toHaveLength(1);expect(x.getData()).toHaveLength(0);});it('disables',()=>{const x=new ErrorReporter();x.disable();x.track('x');expect(x.getMetrics().total).toBe(0);});});