import{describe,it,expect}from"vitest";import{I18nManager}from"../../i18n/core/I18nManager";
describe("I18nManager",()=>{it("creates",()=>expect(new I18nManager()).toBeDefined());it("toggles",()=>{const x=new I18nManager();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new I18nManager();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new I18nManager();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new I18nManager();x.dispose();expect(x.isActive()).toBe(false);});});