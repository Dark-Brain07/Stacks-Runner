import{describe,it,expect}from"vitest";import{RTLSupport}from"../../i18n/core/RTLSupport";
describe("RTLSupport",()=>{it("creates",()=>expect(new RTLSupport()).toBeDefined());it("toggles",()=>{const x=new RTLSupport();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new RTLSupport();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new RTLSupport();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new RTLSupport();x.dispose();expect(x.isActive()).toBe(false);});});