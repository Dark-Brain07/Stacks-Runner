import{describe,it,expect}from"vitest";import{LocaleDetector}from"../../i18n/core/LocaleDetector";
describe("LocaleDetector",()=>{it("creates",()=>expect(new LocaleDetector()).toBeDefined());it("toggles",()=>{const x=new LocaleDetector();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new LocaleDetector();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new LocaleDetector();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new LocaleDetector();x.dispose();expect(x.isActive()).toBe(false);});});