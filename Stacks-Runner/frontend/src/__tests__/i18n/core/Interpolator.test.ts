import{describe,it,expect}from"vitest";import{Interpolator}from"../../i18n/core/Interpolator";
describe("Interpolator",()=>{it("creates",()=>expect(new Interpolator()).toBeDefined());it("toggles",()=>{const x=new Interpolator();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Interpolator();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Interpolator();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Interpolator();x.dispose();expect(x.isActive()).toBe(false);});});