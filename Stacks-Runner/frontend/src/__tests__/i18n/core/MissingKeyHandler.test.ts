import{describe,it,expect}from"vitest";import{MissingKeyHandler}from"../../i18n/core/MissingKeyHandler";
describe("MissingKeyHandler",()=>{it("creates",()=>expect(new MissingKeyHandler()).toBeDefined());it("toggles",()=>{const x=new MissingKeyHandler();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new MissingKeyHandler();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new MissingKeyHandler();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new MissingKeyHandler();x.dispose();expect(x.isActive()).toBe(false);});});