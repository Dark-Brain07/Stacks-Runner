import{describe,it,expect}from'vitest';import{LongPress}from'../../input/LongPress';
describe('LongPress',()=>{it('starts inactive',()=>expect(new LongPress().isRecording()).toBe(false));it('records events',()=>{const x=new LongPress();x.start();x.record({type:'tap'});expect(x.getCount()).toBe(1);});it('clears',()=>{const x=new LongPress();x.start();x.record({type:'tap'});x.clear();expect(x.getCount()).toBe(0);});});