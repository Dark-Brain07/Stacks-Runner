import{describe,it,expect}from'vitest';import{TouchHandler}from'../../input/TouchHandler';
describe('TouchHandler',()=>{it('starts inactive',()=>expect(new TouchHandler().isRecording()).toBe(false));it('records events',()=>{const x=new TouchHandler();x.start();x.record({type:'tap'});expect(x.getCount()).toBe(1);});it('clears',()=>{const x=new TouchHandler();x.start();x.record({type:'tap'});x.clear();expect(x.getCount()).toBe(0);});});