import{describe,it,expect}from"vitest";import{VehicleCustomize}from"../../game/vehicle/VehicleCustomize";
describe("VehicleCustomize",()=>{it("creates",()=>expect(new VehicleCustomize()).toBeDefined());it("toggles",()=>{const x=new VehicleCustomize();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleCustomize();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleCustomize();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleCustomize();x.dispose();expect(x.isActive()).toBe(false);});});