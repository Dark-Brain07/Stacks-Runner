import{describe,it,expect}from"vitest";import{VehicleSound}from"../../game/vehicle/VehicleSound";
describe("VehicleSound",()=>{it("creates",()=>expect(new VehicleSound()).toBeDefined());it("toggles",()=>{const x=new VehicleSound();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleSound();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleSound();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleSound();x.dispose();expect(x.isActive()).toBe(false);});});