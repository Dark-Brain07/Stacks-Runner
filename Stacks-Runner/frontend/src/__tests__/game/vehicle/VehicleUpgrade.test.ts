import{describe,it,expect}from"vitest";import{VehicleUpgrade}from"../../game/vehicle/VehicleUpgrade";
describe("VehicleUpgrade",()=>{it("creates",()=>expect(new VehicleUpgrade()).toBeDefined());it("toggles",()=>{const x=new VehicleUpgrade();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleUpgrade();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleUpgrade();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleUpgrade();x.dispose();expect(x.isActive()).toBe(false);});});