import{describe,it,expect}from"vitest";import{VehicleParticle}from"../../game/vehicle/VehicleParticle";
describe("VehicleParticle",()=>{it("creates",()=>expect(new VehicleParticle()).toBeDefined());it("toggles",()=>{const x=new VehicleParticle();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new VehicleParticle();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new VehicleParticle();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new VehicleParticle();x.dispose();expect(x.isActive()).toBe(false);});});