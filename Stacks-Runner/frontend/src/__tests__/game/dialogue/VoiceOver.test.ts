import{describe,it,expect}from"vitest";import{VoiceOver}from"../../game/dialogue/VoiceOver";
describe("VoiceOver",()=>{it("ok",()=>expect(new VoiceOver()).toBeDefined());it("toggle",()=>{const x=new VoiceOver();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new VoiceOver();x.set("a",1);expect(x.get("a")).toBe(1);});});