import{describe,it,expect}from"vitest";import{DlgManager}from"../../game/dialogue/DlgManager";
describe("DlgManager",()=>{it("ok",()=>expect(new DlgManager()).toBeDefined());it("toggle",()=>{const x=new DlgManager();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new DlgManager();x.set("a",1);expect(x.get("a")).toBe(1);});});