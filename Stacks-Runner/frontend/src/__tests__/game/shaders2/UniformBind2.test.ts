import{describe,it,expect}from"vitest";import{UniformBind2}from"../../game/shaders2/UniformBind2";
describe("UniformBind2",()=>{it("ok",()=>expect(new UniformBind2()).toBeDefined());it("toggle",()=>{const x=new UniformBind2();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new UniformBind2();x.set("a",1);expect(x.get("a")).toBe(1);});});