import{describe,it,expect}from"vitest";import{ChromaAber2}from"../../game/shaders2/ChromaAber2";
describe("ChromaAber2",()=>{it("ok",()=>expect(new ChromaAber2()).toBeDefined());it("toggle",()=>{const x=new ChromaAber2();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new ChromaAber2();x.set("a",1);expect(x.get("a")).toBe(1);});});