import{describe,it,expect}from"vitest";import{DungeonLight}from"../../game/dungeon/DungeonLight";
describe("DungeonLight",()=>{it("creates",()=>expect(new DungeonLight()).toBeDefined());it("toggles",()=>{const x=new DungeonLight();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonLight();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonLight();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonLight();x.dispose();expect(x.isActive()).toBe(false);});});