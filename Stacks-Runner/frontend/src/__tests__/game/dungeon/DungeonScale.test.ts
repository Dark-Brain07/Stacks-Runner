import{describe,it,expect}from"vitest";import{DungeonScale}from"../../game/dungeon/DungeonScale";
describe("DungeonScale",()=>{it("creates",()=>expect(new DungeonScale()).toBeDefined());it("toggles",()=>{const x=new DungeonScale();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonScale();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonScale();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonScale();x.dispose();expect(x.isActive()).toBe(false);});});