import{describe,it,expect}from"vitest";import{DungeonReward}from"../../game/dungeon/DungeonReward";
describe("DungeonReward",()=>{it("creates",()=>expect(new DungeonReward()).toBeDefined());it("toggles",()=>{const x=new DungeonReward();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonReward();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonReward();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonReward();x.dispose();expect(x.isActive()).toBe(false);});});