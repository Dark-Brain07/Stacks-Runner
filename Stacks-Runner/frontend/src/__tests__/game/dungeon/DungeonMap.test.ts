import{describe,it,expect}from"vitest";import{DungeonMap}from"../../game/dungeon/DungeonMap";
describe("DungeonMap",()=>{it("creates",()=>expect(new DungeonMap()).toBeDefined());it("toggles",()=>{const x=new DungeonMap();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DungeonMap();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DungeonMap();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DungeonMap();x.dispose();expect(x.isActive()).toBe(false);});});