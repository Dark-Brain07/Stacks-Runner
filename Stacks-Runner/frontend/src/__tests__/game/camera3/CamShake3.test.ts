import{describe,it,expect}from"vitest";import{CamShake3}from"../../game/camera3/CamShake3";
describe("CamShake3",()=>{it("ok",()=>expect(new CamShake3()).toBeDefined());it("toggle",()=>{const x=new CamShake3();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new CamShake3();x.set("a",1);expect(x.get("a")).toBe(1);});});