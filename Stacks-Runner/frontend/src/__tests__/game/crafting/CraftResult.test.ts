import{describe,it,expect}from"vitest";import{CraftResult}from"../../game/crafting/CraftResult";
describe("CraftResult",()=>{it("creates",()=>expect(new CraftResult()).toBeDefined());it("toggles",()=>{const x=new CraftResult();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CraftResult();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CraftResult();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CraftResult();x.dispose();expect(x.isActive()).toBe(false);});});