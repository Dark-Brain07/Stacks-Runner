import{describe,it,expect}from"vitest";import{CraftPreview}from"../../game/crafting/CraftPreview";
describe("CraftPreview",()=>{it("creates",()=>expect(new CraftPreview()).toBeDefined());it("toggles",()=>{const x=new CraftPreview();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CraftPreview();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CraftPreview();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CraftPreview();x.dispose();expect(x.isActive()).toBe(false);});});