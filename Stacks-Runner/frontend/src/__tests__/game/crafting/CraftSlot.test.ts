import{describe,it,expect}from"vitest";import{CraftSlot}from"../../game/crafting/CraftSlot";
describe("CraftSlot",()=>{it("creates",()=>expect(new CraftSlot()).toBeDefined());it("toggles",()=>{const x=new CraftSlot();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CraftSlot();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CraftSlot();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CraftSlot();x.dispose();expect(x.isActive()).toBe(false);});});