import{describe,it,expect}from"vitest";import{CraftFavorite}from"../../game/crafting/CraftFavorite";
describe("CraftFavorite",()=>{it("creates",()=>expect(new CraftFavorite()).toBeDefined());it("toggles",()=>{const x=new CraftFavorite();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CraftFavorite();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CraftFavorite();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CraftFavorite();x.dispose();expect(x.isActive()).toBe(false);});});