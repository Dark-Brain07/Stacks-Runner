import{describe,it,expect}from"vitest";import{BlueprintDB}from"../../game/crafting/BlueprintDB";
describe("BlueprintDB",()=>{it("creates",()=>expect(new BlueprintDB()).toBeDefined());it("toggles",()=>{const x=new BlueprintDB();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new BlueprintDB();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new BlueprintDB();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new BlueprintDB();x.dispose();expect(x.isActive()).toBe(false);});});