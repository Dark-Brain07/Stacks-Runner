import{describe,it,expect}from"vitest";import{PriceHistory}from"../../game/economy/PriceHistory";
describe("PriceHistory",()=>{it("creates",()=>expect(new PriceHistory()).toBeDefined());it("toggles",()=>{const x=new PriceHistory();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PriceHistory();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PriceHistory();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PriceHistory();x.dispose();expect(x.isActive()).toBe(false);});});