import{describe,it,expect}from"vitest";import{LoanSystem}from"../../game/economy/LoanSystem";
describe("LoanSystem",()=>{it("creates",()=>expect(new LoanSystem()).toBeDefined());it("toggles",()=>{const x=new LoanSystem();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new LoanSystem();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new LoanSystem();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new LoanSystem();x.dispose();expect(x.isActive()).toBe(false);});});