import{describe,it,expect}from"vitest";import{Supply}from"../../game/economy/Supply";
describe("Supply",()=>{it("creates",()=>expect(new Supply()).toBeDefined());it("toggles",()=>{const x=new Supply();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Supply();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Supply();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Supply();x.dispose();expect(x.isActive()).toBe(false);});});