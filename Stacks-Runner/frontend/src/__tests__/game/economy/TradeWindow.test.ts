import{describe,it,expect}from"vitest";import{TradeWindow}from"../../game/economy/TradeWindow";
describe("TradeWindow",()=>{it("creates",()=>expect(new TradeWindow()).toBeDefined());it("toggles",()=>{const x=new TradeWindow();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new TradeWindow();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new TradeWindow();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new TradeWindow();x.dispose();expect(x.isActive()).toBe(false);});});