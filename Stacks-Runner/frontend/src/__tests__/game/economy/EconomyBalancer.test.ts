import{describe,it,expect}from"vitest";import{EconomyBalancer}from"../../game/economy/EconomyBalancer";
describe("EconomyBalancer",()=>{it("creates",()=>expect(new EconomyBalancer()).toBeDefined());it("toggles",()=>{const x=new EconomyBalancer();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new EconomyBalancer();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new EconomyBalancer();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new EconomyBalancer();x.dispose();expect(x.isActive()).toBe(false);});});