import{describe,it,expect}from"vitest";import{Spring3}from"../../game/physics4/Spring3";
describe("Spring3",()=>{it("ok",()=>expect(new Spring3()).toBeDefined());it("toggle",()=>{const x=new Spring3();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new Spring3();x.set("a",1);expect(x.get("a")).toBe(1);});});