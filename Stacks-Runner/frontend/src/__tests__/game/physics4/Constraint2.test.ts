import{describe,it,expect}from"vitest";import{Constraint2}from"../../game/physics4/Constraint2";
describe("Constraint2",()=>{it("ok",()=>expect(new Constraint2()).toBeDefined());it("toggle",()=>{const x=new Constraint2();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new Constraint2();x.set("a",1);expect(x.get("a")).toBe(1);});});