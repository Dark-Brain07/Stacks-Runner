import{describe,it,expect}from"vitest";import{Raycast2}from"../../game/physics4/Raycast2";
describe("Raycast2",()=>{it("ok",()=>expect(new Raycast2()).toBeDefined());it("toggle",()=>{const x=new Raycast2();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new Raycast2();x.set("a",1);expect(x.get("a")).toBe(1);});});