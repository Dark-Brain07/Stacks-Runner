import{describe,it,expect}from"vitest";import{SkillAnimation}from"../../game/skills/SkillAnimation";
describe("SkillAnimation",()=>{it("creates",()=>expect(new SkillAnimation()).toBeDefined());it("toggles",()=>{const x=new SkillAnimation();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SkillAnimation();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SkillAnimation();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SkillAnimation();x.dispose();expect(x.isActive()).toBe(false);});});