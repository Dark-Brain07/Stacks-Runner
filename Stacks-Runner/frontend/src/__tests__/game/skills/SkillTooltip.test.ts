import{describe,it,expect}from"vitest";import{SkillTooltip}from"../../game/skills/SkillTooltip";
describe("SkillTooltip",()=>{it("creates",()=>expect(new SkillTooltip()).toBeDefined());it("toggles",()=>{const x=new SkillTooltip();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SkillTooltip();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SkillTooltip();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SkillTooltip();x.dispose();expect(x.isActive()).toBe(false);});});