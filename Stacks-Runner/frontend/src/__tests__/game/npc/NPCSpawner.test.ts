import{describe,it,expect}from"vitest";import{NPCSpawner}from"../../game/npc/NPCSpawner";
describe("NPCSpawner",()=>{it("creates",()=>expect(new NPCSpawner()).toBeDefined());it("toggles",()=>{const x=new NPCSpawner();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new NPCSpawner();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new NPCSpawner();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new NPCSpawner();x.dispose();expect(x.isActive()).toBe(false);});});