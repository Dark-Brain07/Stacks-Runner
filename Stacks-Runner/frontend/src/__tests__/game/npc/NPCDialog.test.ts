import{describe,it,expect}from"vitest";import{NPCDialog}from"../../game/npc/NPCDialog";
describe("NPCDialog",()=>{it("creates",()=>expect(new NPCDialog()).toBeDefined());it("toggles",()=>{const x=new NPCDialog();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new NPCDialog();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new NPCDialog();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new NPCDialog();x.dispose();expect(x.isActive()).toBe(false);});});