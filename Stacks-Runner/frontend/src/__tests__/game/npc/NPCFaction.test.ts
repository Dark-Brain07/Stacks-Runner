import{describe,it,expect}from"vitest";import{NPCFaction}from"../../game/npc/NPCFaction";
describe("NPCFaction",()=>{it("creates",()=>expect(new NPCFaction()).toBeDefined());it("toggles",()=>{const x=new NPCFaction();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new NPCFaction();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new NPCFaction();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new NPCFaction();x.dispose();expect(x.isActive()).toBe(false);});});