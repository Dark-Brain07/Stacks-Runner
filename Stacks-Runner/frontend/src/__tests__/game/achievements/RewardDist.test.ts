import{describe,it,expect}from"vitest";import{RewardDist}from"../../game/achievements/RewardDist";
describe("RewardDist",()=>{it("ok",()=>expect(new RewardDist()).toBeDefined());it("toggle",()=>{const x=new RewardDist();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new RewardDist();x.set("a",1);expect(x.get("a")).toBe(1);});});