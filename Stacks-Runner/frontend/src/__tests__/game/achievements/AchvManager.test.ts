import{describe,it,expect}from"vitest";import{AchvManager}from"../../game/achievements/AchvManager";
describe("AchvManager",()=>{it("ok",()=>expect(new AchvManager()).toBeDefined());it("toggle",()=>{const x=new AchvManager();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new AchvManager();x.set("a",1);expect(x.get("a")).toBe(1);});});