import { describe, it, expect } from 'vitest'; import { TutorialController } from '../../game/tutorial/TutorialController';
describe('TutorialController', () => { beforeEach(() => localStorage.clear()); it('starts at welcome', () => expect(new TutorialController().getCurrent()?.id).toBe('welcome')); it('advances', () => { const t = new TutorialController(); t.next(); expect(t.getCurrent()?.id).toBe('move'); }); it('completes', () => { const t = new TutorialController(); for (let i = 0; i < 8; i++) t.next(); expect(t.isComplete()).toBe(true); }); it('skips', () => { const t = new TutorialController(); t.skip(); expect(t.isComplete()).toBe(true); }); });