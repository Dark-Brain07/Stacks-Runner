import { describe, it, expect } from 'vitest'; import { POWERUPS, getPowerUpById, getRandomPowerUp } from '../../game/powerups/definitions';
describe('power-up definitions', () => { it('has 6 power-ups', () => expect(POWERUPS).toHaveLength(6)); it('finds by id', () => expect(getPowerUpById('shield')?.name).toBe('Shield')); it('returns undefined for invalid', () => expect(getPowerUpById('x')).toBeUndefined()); it('returns random', () => expect(POWERUPS).toContain(getRandomPowerUp())); it('each has fields', () => POWERUPS.forEach(p => { expect(p.id).toBeTruthy(); expect(p.duration).toBeGreaterThan(0); })); });