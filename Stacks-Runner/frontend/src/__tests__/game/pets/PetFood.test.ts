import{describe,it,expect}from"vitest";import{PetFood}from"../../game/pets/PetFood";
describe("PetFood",()=>{it("creates",()=>expect(new PetFood()).toBeDefined());it("toggles",()=>{const x=new PetFood();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PetFood();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PetFood();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PetFood();x.dispose();expect(x.isActive()).toBe(false);});});