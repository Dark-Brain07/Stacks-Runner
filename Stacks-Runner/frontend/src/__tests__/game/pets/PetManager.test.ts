import{describe,it,expect}from"vitest";import{PetManager}from"../../game/pets/PetManager";
describe("PetManager",()=>{it("creates",()=>expect(new PetManager()).toBeDefined());it("toggles",()=>{const x=new PetManager();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PetManager();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PetManager();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PetManager();x.dispose();expect(x.isActive()).toBe(false);});});