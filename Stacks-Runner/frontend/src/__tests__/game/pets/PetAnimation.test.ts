import{describe,it,expect}from"vitest";import{PetAnimation}from"../../game/pets/PetAnimation";
describe("PetAnimation",()=>{it("creates",()=>expect(new PetAnimation()).toBeDefined());it("toggles",()=>{const x=new PetAnimation();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PetAnimation();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PetAnimation();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PetAnimation();x.dispose();expect(x.isActive()).toBe(false);});});