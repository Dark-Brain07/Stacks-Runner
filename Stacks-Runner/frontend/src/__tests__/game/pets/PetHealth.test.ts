import{describe,it,expect}from"vitest";import{PetHealth}from"../../game/pets/PetHealth";
describe("PetHealth",()=>{it("creates",()=>expect(new PetHealth()).toBeDefined());it("toggles",()=>{const x=new PetHealth();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PetHealth();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PetHealth();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PetHealth();x.dispose();expect(x.isActive()).toBe(false);});});