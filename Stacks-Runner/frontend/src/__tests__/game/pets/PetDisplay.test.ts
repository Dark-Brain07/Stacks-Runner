import{describe,it,expect}from"vitest";import{PetDisplay}from"../../game/pets/PetDisplay";
describe("PetDisplay",()=>{it("creates",()=>expect(new PetDisplay()).toBeDefined());it("toggles",()=>{const x=new PetDisplay();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PetDisplay();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PetDisplay();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PetDisplay();x.dispose();expect(x.isActive()).toBe(false);});});