import{describe,it,expect}from"vitest";import{FogLayer}from"../../game/terrain/FogLayer";
describe("FogLayer",()=>{it("ok",()=>expect(new FogLayer()).toBeDefined());it("toggle",()=>{const x=new FogLayer();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new FogLayer();x.set("a",1);expect(x.get("a")).toBe(1);});});