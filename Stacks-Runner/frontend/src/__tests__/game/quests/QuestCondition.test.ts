import{describe,it,expect}from"vitest";import{QuestCondition}from"../../game/quests/QuestCondition";
describe("QuestCondition",()=>{it("creates",()=>expect(new QuestCondition()).toBeDefined());it("toggles",()=>{const x=new QuestCondition();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestCondition();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestCondition();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestCondition();x.dispose();expect(x.isActive()).toBe(false);});});