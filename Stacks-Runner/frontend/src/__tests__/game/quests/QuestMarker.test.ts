import{describe,it,expect}from"vitest";import{QuestMarker}from"../../game/quests/QuestMarker";
describe("QuestMarker",()=>{it("creates",()=>expect(new QuestMarker()).toBeDefined());it("toggles",()=>{const x=new QuestMarker();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestMarker();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestMarker();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestMarker();x.dispose();expect(x.isActive()).toBe(false);});});