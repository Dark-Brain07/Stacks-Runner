import{describe,it,expect}from"vitest";import{QuestManager}from"../../game/quests/QuestManager";
describe("QuestManager",()=>{it("creates",()=>expect(new QuestManager()).toBeDefined());it("toggles",()=>{const x=new QuestManager();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestManager();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestManager();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestManager();x.dispose();expect(x.isActive()).toBe(false);});});