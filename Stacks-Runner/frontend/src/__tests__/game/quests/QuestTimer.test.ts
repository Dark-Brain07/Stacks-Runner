import{describe,it,expect}from"vitest";import{QuestTimer}from"../../game/quests/QuestTimer";
describe("QuestTimer",()=>{it("creates",()=>expect(new QuestTimer()).toBeDefined());it("toggles",()=>{const x=new QuestTimer();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestTimer();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestTimer();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestTimer();x.dispose();expect(x.isActive()).toBe(false);});});