import{describe,it,expect}from"vitest";import{QuestState}from"../../game/quests/QuestState";
describe("QuestState",()=>{it("creates",()=>expect(new QuestState()).toBeDefined());it("toggles",()=>{const x=new QuestState();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new QuestState();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new QuestState();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new QuestState();x.dispose();expect(x.isActive()).toBe(false);});});