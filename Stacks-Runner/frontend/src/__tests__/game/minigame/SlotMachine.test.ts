import{describe,it,expect}from"vitest";import{SlotMachine}from"../../game/minigame/SlotMachine";
describe("SlotMachine",()=>{it("creates",()=>expect(new SlotMachine()).toBeDefined());it("toggles",()=>{const x=new SlotMachine();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new SlotMachine();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new SlotMachine();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new SlotMachine();x.dispose();expect(x.isActive()).toBe(false);});});