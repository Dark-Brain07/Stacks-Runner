import{describe,it,expect}from"vitest";import{RhythmGame}from"../../game/minigame/RhythmGame";
describe("RhythmGame",()=>{it("creates",()=>expect(new RhythmGame()).toBeDefined());it("toggles",()=>{const x=new RhythmGame();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new RhythmGame();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new RhythmGame();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new RhythmGame();x.dispose();expect(x.isActive()).toBe(false);});});