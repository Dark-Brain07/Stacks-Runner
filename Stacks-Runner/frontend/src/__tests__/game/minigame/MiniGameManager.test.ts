import{describe,it,expect}from"vitest";import{MiniGameManager}from"../../game/minigame/MiniGameManager";
describe("MiniGameManager",()=>{it("creates",()=>expect(new MiniGameManager()).toBeDefined());it("toggles",()=>{const x=new MiniGameManager();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new MiniGameManager();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new MiniGameManager();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new MiniGameManager();x.dispose();expect(x.isActive()).toBe(false);});});