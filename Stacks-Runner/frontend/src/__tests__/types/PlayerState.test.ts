import{describe,it,expect}from'vitest';import{createPlayerState,updatePlayerState}from'../../types/PlayerState';
describe('PlayerState',()=>{it('creates',()=>{const s=createPlayerState('test');expect(s.id).toBe('test');expect(s.active).toBe(true);});});