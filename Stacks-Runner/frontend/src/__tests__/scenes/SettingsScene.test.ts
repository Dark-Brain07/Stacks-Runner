import{describe,it,expect}from'vitest';import{SettingsScene}from'../../scenes/SettingsScene';
describe('SettingsScene',()=>{it('starts inactive',()=>expect(new SettingsScene().isActive()).toBe(false));it('activates on enter',()=>{const s=new SettingsScene();s.onEnter();expect(s.isActive()).toBe(true);});it('deactivates on exit',()=>{const s=new SettingsScene();s.onEnter();s.onExit();expect(s.isActive()).toBe(false);});});