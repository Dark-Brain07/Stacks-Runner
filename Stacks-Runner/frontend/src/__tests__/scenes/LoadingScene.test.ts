import{describe,it,expect}from'vitest';import{LoadingScene}from'../../scenes/LoadingScene';
describe('LoadingScene',()=>{it('starts inactive',()=>expect(new LoadingScene().isActive()).toBe(false));it('activates on enter',()=>{const s=new LoadingScene();s.onEnter();expect(s.isActive()).toBe(true);});it('deactivates on exit',()=>{const s=new LoadingScene();s.onEnter();s.onExit();expect(s.isActive()).toBe(false);});});