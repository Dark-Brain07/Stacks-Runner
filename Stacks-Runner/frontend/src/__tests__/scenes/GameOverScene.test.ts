import{describe,it,expect}from'vitest';import{GameOverScene}from'../../scenes/GameOverScene';
describe('GameOverScene',()=>{it('starts inactive',()=>expect(new GameOverScene().isActive()).toBe(false));it('activates on enter',()=>{const s=new GameOverScene();s.onEnter();expect(s.isActive()).toBe(true);});it('deactivates on exit',()=>{const s=new GameOverScene();s.onEnter();s.onExit();expect(s.isActive()).toBe(false);});});