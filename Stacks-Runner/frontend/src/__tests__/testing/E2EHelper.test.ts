import{describe,it,expect}from'vitest';import{E2EHelper}from'../../testing/E2EHelper';
describe('E2EHelper',()=>{it('runs tests',()=>{const x=new E2EHelper();const r=x.run('test1',()=>{});expect(r.pass).toBe(true);});it('tracks results',()=>{const x=new E2EHelper();x.run('a',()=>{});x.run('b',()=>{throw new Error();});expect(x.getSummary().passed).toBe(1);expect(x.getSummary().failed).toBe(1);});});