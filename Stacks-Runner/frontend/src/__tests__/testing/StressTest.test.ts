import{describe,it,expect}from'vitest';import{StressTest}from'../../testing/StressTest';
describe('StressTest',()=>{it('runs tests',()=>{const x=new StressTest();const r=x.run('test1',()=>{});expect(r.pass).toBe(true);});it('tracks results',()=>{const x=new StressTest();x.run('a',()=>{});x.run('b',()=>{throw new Error();});expect(x.getSummary().passed).toBe(1);expect(x.getSummary().failed).toBe(1);});});