import{describe,it,expect}from"vitest";import{PrioQueue2}from"../../services/notif3/PrioQueue2";
describe("PrioQueue2",()=>{it("ok",()=>expect(new PrioQueue2()).toBeDefined());it("toggle",()=>{const x=new PrioQueue2();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new PrioQueue2();x.set("a",1);expect(x.get("a")).toBe(1);});});