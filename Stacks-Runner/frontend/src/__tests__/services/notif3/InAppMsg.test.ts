import{describe,it,expect}from"vitest";import{InAppMsg}from"../../services/notif3/InAppMsg";
describe("InAppMsg",()=>{it("ok",()=>expect(new InAppMsg()).toBeDefined());it("toggle",()=>{const x=new InAppMsg();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new InAppMsg();x.set("a",1);expect(x.get("a")).toBe(1);});});