import { describe, it, expect } from "vitest"; import { getAchievements, unlockAchievement, getUnlockedCount } from "../../services/achievements"; describe("achievements", () => { beforeEach(() => localStorage.clear()); it("returns all achievements", () => { expect(getAchievements().length).toBe(10); }); it("unlocks achievement", () => { const result = unlockAchievement("first_game"); expect(result).not.toBeNull(); expect(result?.unlocked).toBe(true); }); it("returns null for already unlocked", () => { unlockAchievement("first_game"); expect(unlockAchievement("first_game")).toBeNull(); }); it("counts unlocked", () => { unlockAchievement("first_game"); unlockAchievement("score_100"); expect(getUnlockedCount()).toBe(2); }); });
