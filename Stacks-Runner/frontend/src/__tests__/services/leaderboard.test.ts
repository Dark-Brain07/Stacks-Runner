import { describe, it, expect } from "vitest"; import { getLeaderboard, addToLeaderboard, getPlayerRank, clearLeaderboard } from "../../services/leaderboard"; describe("leaderboard service", () => { beforeEach(() => { localStorage.clear(); clearLeaderboard(); }); it("starts empty", () => { expect(getLeaderboard()).toEqual([]); }); it("adds entries sorted by score", () => { addToLeaderboard("SP1", 100); addToLeaderboard("SP2", 500); const board = getLeaderboard(); expect(board[0].score).toBe(500); }); it("returns player rank", () => { addToLeaderboard("SP1", 100); addToLeaderboard("SP2", 500); expect(getPlayerRank("SP2")).toBe(1); expect(getPlayerRank("SP1")).toBe(2); }); it("returns -1 for unknown player", () => { expect(getPlayerRank("SPX")).toBe(-1); }); it("limits to 50 entries", () => { for (let i = 0; i < 60; i++) addToLeaderboard("SP" + i, i); expect(getLeaderboard().length).toBe(50); }); });
