import{describe,it,expect}from"vitest";import{PlayerSync}from"../../services/multiplayer/PlayerSync";
describe("PlayerSync",()=>{it("ok",()=>expect(new PlayerSync()).toBeDefined());it("toggle",()=>{const x=new PlayerSync();x.disable();expect(x.isActive()).toBe(false);});it("data",()=>{const x=new PlayerSync();x.set("a",1);expect(x.get("a")).toBe(1);});});