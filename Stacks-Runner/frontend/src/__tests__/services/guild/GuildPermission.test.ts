import{describe,it,expect}from"vitest";import{GuildPermission}from"../../services/guild/GuildPermission";
describe("GuildPermission",()=>{it("creates",()=>expect(new GuildPermission()).toBeDefined());it("toggles",()=>{const x=new GuildPermission();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new GuildPermission();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new GuildPermission();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new GuildPermission();x.dispose();expect(x.isActive()).toBe(false);});});