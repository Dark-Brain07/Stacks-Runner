import{describe,it,expect}from"vitest";import{GuildChat}from"../../services/guild/GuildChat";
describe("GuildChat",()=>{it("creates",()=>expect(new GuildChat()).toBeDefined());it("toggles",()=>{const x=new GuildChat();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new GuildChat();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new GuildChat();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new GuildChat();x.dispose();expect(x.isActive()).toBe(false);});});