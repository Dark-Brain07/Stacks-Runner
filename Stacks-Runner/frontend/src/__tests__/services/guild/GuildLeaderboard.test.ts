import{describe,it,expect}from"vitest";import{GuildLeaderboard}from"../../services/guild/GuildLeaderboard";
describe("GuildLeaderboard",()=>{it("creates",()=>expect(new GuildLeaderboard()).toBeDefined());it("toggles",()=>{const x=new GuildLeaderboard();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new GuildLeaderboard();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new GuildLeaderboard();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new GuildLeaderboard();x.dispose();expect(x.isActive()).toBe(false);});});