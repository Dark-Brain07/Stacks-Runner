import{describe,it,expect}from"vitest";import{GuildDiplomacy}from"../../services/guild/GuildDiplomacy";
describe("GuildDiplomacy",()=>{it("creates",()=>expect(new GuildDiplomacy()).toBeDefined());it("toggles",()=>{const x=new GuildDiplomacy();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new GuildDiplomacy();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new GuildDiplomacy();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new GuildDiplomacy();x.dispose();expect(x.isActive()).toBe(false);});});