import{describe,it,expect}from"vitest";import{GuildRank}from"../../services/guild/GuildRank";
describe("GuildRank",()=>{it("creates",()=>expect(new GuildRank()).toBeDefined());it("toggles",()=>{const x=new GuildRank();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new GuildRank();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new GuildRank();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new GuildRank();x.dispose();expect(x.isActive()).toBe(false);});});