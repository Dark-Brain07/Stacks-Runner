import{describe,it,expect}from"vitest";import{Invitation}from"../../services/social/Invitation";
describe("Invitation",()=>{it("creates",()=>expect(new Invitation()).toBeDefined());it("toggles",()=>{const x=new Invitation();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Invitation();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Invitation();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Invitation();x.dispose();expect(x.isActive()).toBe(false);});});