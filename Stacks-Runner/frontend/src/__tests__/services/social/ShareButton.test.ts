import{describe,it,expect}from"vitest";import{ShareButton}from"../../services/social/ShareButton";
describe("ShareButton",()=>{it("creates",()=>expect(new ShareButton()).toBeDefined());it("toggles",()=>{const x=new ShareButton();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new ShareButton();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new ShareButton();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new ShareButton();x.dispose();expect(x.isActive()).toBe(false);});});