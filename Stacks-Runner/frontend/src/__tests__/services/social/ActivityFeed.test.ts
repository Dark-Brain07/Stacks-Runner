import{describe,it,expect}from"vitest";import{ActivityFeed}from"../../services/social/ActivityFeed";
describe("ActivityFeed",()=>{it("creates",()=>expect(new ActivityFeed()).toBeDefined());it("toggles",()=>{const x=new ActivityFeed();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new ActivityFeed();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new ActivityFeed();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new ActivityFeed();x.dispose();expect(x.isActive()).toBe(false);});});