import{describe,it,expect}from"vitest";import{PartySystem}from"../../services/social/PartySystem";
describe("PartySystem",()=>{it("creates",()=>expect(new PartySystem()).toBeDefined());it("toggles",()=>{const x=new PartySystem();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new PartySystem();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new PartySystem();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new PartySystem();x.dispose();expect(x.isActive()).toBe(false);});});