import{describe,it,expect}from"vitest";import{FriendList}from"../../services/social/FriendList";
describe("FriendList",()=>{it("creates",()=>expect(new FriendList()).toBeDefined());it("toggles",()=>{const x=new FriendList();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new FriendList();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new FriendList();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new FriendList();x.dispose();expect(x.isActive()).toBe(false);});});