import{describe,it,expect}from"vitest";import{Referral}from"../../services/social/Referral";
describe("Referral",()=>{it("creates",()=>expect(new Referral()).toBeDefined());it("toggles",()=>{const x=new Referral();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Referral();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Referral();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Referral();x.dispose();expect(x.isActive()).toBe(false);});});