import{describe,it,expect}from"vitest";import{FriendRequest}from"../../services/social/FriendRequest";
describe("FriendRequest",()=>{it("creates",()=>expect(new FriendRequest()).toBeDefined());it("toggles",()=>{const x=new FriendRequest();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new FriendRequest();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new FriendRequest();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new FriendRequest();x.dispose();expect(x.isActive()).toBe(false);});});