import{describe,it,expect}from"vitest";import{UserSearch}from"../../services/social/UserSearch";
describe("UserSearch",()=>{it("creates",()=>expect(new UserSearch()).toBeDefined());it("toggles",()=>{const x=new UserSearch();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new UserSearch();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new UserSearch();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new UserSearch();x.dispose();expect(x.isActive()).toBe(false);});});