import{describe,it,expect}from"vitest";import{OnlineStatus}from"../../services/social/OnlineStatus";
describe("OnlineStatus",()=>{it("creates",()=>expect(new OnlineStatus()).toBeDefined());it("toggles",()=>{const x=new OnlineStatus();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new OnlineStatus();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new OnlineStatus();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new OnlineStatus();x.dispose();expect(x.isActive()).toBe(false);});});