import{describe,it,expect}from"vitest";import{CPUUsage}from"../../services/monitoring/CPUUsage";
describe("CPUUsage",()=>{it("creates",()=>expect(new CPUUsage()).toBeDefined());it("toggles",()=>{const x=new CPUUsage();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new CPUUsage();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new CPUUsage();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new CPUUsage();x.dispose();expect(x.isActive()).toBe(false);});});