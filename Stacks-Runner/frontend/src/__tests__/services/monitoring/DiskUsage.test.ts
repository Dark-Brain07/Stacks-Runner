import{describe,it,expect}from"vitest";import{DiskUsage}from"../../services/monitoring/DiskUsage";
describe("DiskUsage",()=>{it("creates",()=>expect(new DiskUsage()).toBeDefined());it("toggles",()=>{const x=new DiskUsage();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new DiskUsage();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new DiskUsage();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new DiskUsage();x.dispose();expect(x.isActive()).toBe(false);});});