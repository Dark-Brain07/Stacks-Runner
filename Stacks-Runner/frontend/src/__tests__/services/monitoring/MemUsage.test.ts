import{describe,it,expect}from"vitest";import{MemUsage}from"../../services/monitoring/MemUsage";
describe("MemUsage",()=>{it("creates",()=>expect(new MemUsage()).toBeDefined());it("toggles",()=>{const x=new MemUsage();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new MemUsage();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new MemUsage();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new MemUsage();x.dispose();expect(x.isActive()).toBe(false);});});