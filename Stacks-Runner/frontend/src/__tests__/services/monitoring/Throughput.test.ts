import{describe,it,expect}from"vitest";import{Throughput}from"../../services/monitoring/Throughput";
describe("Throughput",()=>{it("creates",()=>expect(new Throughput()).toBeDefined());it("toggles",()=>{const x=new Throughput();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Throughput();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Throughput();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Throughput();x.dispose();expect(x.isActive()).toBe(false);});});