import{describe,it,expect}from"vitest";import{Percentile}from"../../services/monitoring/Percentile";
describe("Percentile",()=>{it("creates",()=>expect(new Percentile()).toBeDefined());it("toggles",()=>{const x=new Percentile();x.disable();expect(x.isActive()).toBe(false);x.enable();expect(x.isActive()).toBe(true);});it("stores",()=>{const x=new Percentile();x.set("k",42);expect(x.get("k")).toBe(42);});it("resets",()=>{const x=new Percentile();x.set("k",1);x.reset();expect(x.get("k")).toBeUndefined();});it("disposes",()=>{const x=new Percentile();x.dispose();expect(x.isActive()).toBe(false);});});