import React from "react"; import { getUnlockedCount } from "../services/achievements"; import { getLeaderboard } from "../services/leaderboard"; import { getDailyStreak } from "../services/dailyChallenge"; export const StatsCard: React.FC = () => { const achievements = getUnlockedCount(); const games = getLeaderboard().length; const streak = getDailyStreak(); const stats = [ { label: "Games Played", value: games.toString() }, { label: "Achievements", value: achievements + "/10" }, { label: "Daily Streak", value: streak + " days" } ]; return React.createElement("div", { className: "grid grid-cols-3 gap-3" }, stats.map(s => React.createElement("div", { key: s.label, className: "bg-gray-800/60 rounded-lg p-3 text-center" }, React.createElement("div", { className: "text-lg font-bold text-white" }, s.value), React.createElement("div", { className: "text-xs text-gray-400" }, s.label)))); };