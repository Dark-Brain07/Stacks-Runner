import React from "react"; import { getLeaderboard } from "../services/leaderboard"; import { truncateAddress } from "../utils/format"; export const LeaderboardTable: React.FC<{ currentAddress?: string }> = ({ currentAddress }) => { const entries = getLeaderboard().slice(0, 10); return React.createElement("div", { className: "bg-gray-800/80 rounded-xl overflow-hidden" }, React.createElement("div", { className: "p-3 border-b border-gray-700" }, React.createElement("h3", { className: "text-sm font-bold text-white" }, "Top Players")), entries.length === 0 ? React.createElement("p", { className: "p-4 text-gray-500 text-center text-sm" }, "No scores yet") : React.createElement("div", null, entries.map((entry, i) => React.createElement("div", { key: i, className: "flex items-center justify-between px-3 py-2 text-sm " + (entry.address === currentAddress ? "bg-purple-900/30" : i % 2 === 0 ? "bg-gray-900/30" : "") }, React.createElement("div", { className: "flex items-center gap-2" }, React.createElement("span", { className: "text-gray-500 w-5" }, "#" + (i + 1)), React.createElement("span", { className: "text-gray-300" }, truncateAddress(entry.address))), React.createElement("span", { className: "font-mono text-white" }, entry.score.toLocaleString()))))); };