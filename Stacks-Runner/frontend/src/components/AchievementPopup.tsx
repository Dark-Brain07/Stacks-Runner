import React, { useEffect, useState } from "react"; interface Props { title: string; description: string; onDismiss: () => void; } export const AchievementPopup: React.FC<Props> = ({ title, description, onDismiss }) => { const [visible, setVisible] = useState(false); useEffect(() => { setTimeout(() => setVisible(true), 100); const timer = setTimeout(() => { setVisible(false); setTimeout(onDismiss, 500); }, 4000); return () => clearTimeout(timer); }, [onDismiss]); return React.createElement("div", { className: "fixed top-4 right-4 z-50 transition-all duration-500 " + (visible ? "opacity-100 translate-x-0" : "opacity-0 translate-x-20") }, React.createElement("div", { className: "bg-gradient-to-r from-yellow-600 to-amber-500 rounded-lg p-4 shadow-xl flex items-center gap-3" }, React.createElement("span", { className: "text-2xl" }, "ðŸ†"), React.createElement("div", null, React.createElement("div", { className: "font-bold text-white text-sm" }, title), React.createElement("div", { className: "text-yellow-100 text-xs" }, description)))); };